__pycache__/
*.pyc
.pytest_cache/
.venv/
build/
dist/
*.egg-info/
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/main.pdf

# Paper source is kept locally and managed in Overleaf, not in this repository.
paper/

# Checks against documents this repository does not ship. Quoting their text here
# would put it in a public repository by the back door; see the docstring in
# tests/test_docs_numbers.py::_load_local_claims.
tests/local_claims.py

# Overleaf staging and the built package. Paper material, same rule as paper/.
output/

# Scratch space and stray pdflatex output from running a build at the repo root.
tmp/
texput.log
