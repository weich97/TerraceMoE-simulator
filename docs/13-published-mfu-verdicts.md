# 13a. Verifier findings behind docs/13

Source and arithmetic checks on each extracted run. Kept so the drops and
corrections in docs/13 can be audited rather than trusted.

[confirmed] DeepSeek-V2 (pre-training run) [deepseek-qwen]: Every quote verified in arXiv:2405.04434 HTML. 236B/21B, 60 layers, hidden 5120, 160 routed + 2 shared, expert intermediate 1536, top-6, M=3, 8.1T tokens, 4K seq, batch 2304->9216 over first 225B, PP=16 zero-bubble + EP=8 + ZeRO-1 DP, no TP, H800 NVLink/NVSwitch/IB, 172.8K vs 300.6K GPU-h per trillion tokens, 'relatively high MFU' with no number, >50K tok/s single-node generation, +1000 steps at 32K/576 seqs. Critically: I searched the paper for a GPU count and line 452 reads only 'cluster equipped with NVIDIA H800 GPUs' with no number — the entry's refusal to infer a count is correct. Derived 1,607 tokens/GPU/s checks out (1e12/172,800/3600 = 1607.2).
[confirmed] DeepSeek-V3 (pre-training run) [deepseek-qwen]: Both sources verified. Tech report (2412.19437): case-insensitive search for MFU/'model flops utilization'/'hardware flops' returns exactly 0 hits, confirming the negative finding; 671B/37B, 61 layers, 7168, 256+1 experts, 2048 intermediate, top-8, <=4 nodes, 14.8T, 4K, 3072->15360 over 469B, 2048 H800, PP16/EP64/ZeRO-1, no TP, 180K GPU-h/T, 2.664M/2.788M totals all verified. Hardware paper (2505.09343) Table 4 verified exactly: 272.80/272.52 tokens-day, 19.926/19.946 s/step, TFLOPS 432/385, MFU 43.73%/38.94% MPFT and 43.68%/38.90% MRFT, plus 'MFU ... is calculated based on BF16 peak performance'. Both derivations reproduce: 272.80e9/86400 x 19.926 / 4096 = 15,360 sequences exactly; 180K GPU-h implies 273.1B tokens/day vs measured 272.80B. The TFLOPS-is-per-GPU inference (432/0.4373 = 988) is sound and flagged as an inference.
[confirmed] DeepSeek-R1-Zero (RL run on DeepSeek-V3-Base) [deepseek-qwen]: arXiv:2501.12948 verified. '64*8 H800 GPUs ... approximately 198 hours', Table 7 101K H800 GPU-hours / $202K, 10,400 steps at 1.6 epochs, 32 questions -> batch 512, 8,192 rollouts split into 16 mini-batches, 32,768 tokens before step 8.2k and 65,536 after, A100s only for the 30B preparatory model. No MFU/HFU/TFLOPS/tokens-per-second anywhere. The flagged internal inconsistency is real and I reproduced both sides: line 1173 says 'scale up to 660B R1-Zero and R1' while the results table prints '671B'. EP-for-rollout and DualPipe quotes verified verbatim.
[confirmed] DeepSeek-R1 (cold-start SFT + two-stage RL) [deepseek-qwen]: arXiv:2501.12948 verified. 'the same 64*8 H800 GPUs, completing the process in about 4 days, or roughly 80 hours. To create the SFT datasets, we use 5K GPU hours.' Table 7 row values 101K / 5K / 41K / 147K and $202K / $10K / $82K / $294K all verified. Stage-1 RL hyperparameters and the 1,700-step stage-2 with general/preference data only in the final 400 steps verified. The SFT figures (2-3 epochs, max context 32,768, batch 128) are correctly attributed — they sit in Appendix B.4.2 'Hyper-Parameters of SFT' for cold-start and second-stage SFT, distinct from B.4.3 Distillation which uses batch 64.
[confirmed] DeepSeek LLM 67B (DENSE contrast) [deepseek-qwen]: arXiv:2401.02954 verified: 95 layers for 67B, Table 2 row (8192 d_model, 64/8 heads, 4096 context, 4608 sequence batch, 2.0T tokens), FFN intermediate '(8/3)d_model' verified verbatim, HAI-LLM with data/tensor/sequence/1F1B pipeline parallelism and ZeRO-1. Negative finding verified: zero hits for A100, H800 or TFLOPS in the report, so no accelerator type or count for this run. The 300.6K GPU-h/T figure is correctly sourced to the DeepSeek-V2 paper, not this one. Derived 924 tokens/GPU/s checks out (1e12/300,600/3600 = 924.0).
[corrected] Qwen2-57B-A14B (pre-training run) [deepseek-qwen]: Report claims all verified in arXiv:2407.10671: 57B/14B, Table 1 (hidden 3,584, 28 layers, intermediate 2,560, 64 routed, 8 activated, 8 shared, vocab 151,646, 4.5T trained tokens), 'upscaled from Qwen2-7B', 4,096->32,768 in the concluding phase. Negative findings verified: zero hits for A100/H100/H800/TFLOPS/Megatron/cluster; the lone GPU/NPU/Ascend/MFU keyword hits are substring artefacts ('ascended', 'harmful'), so 'no hardware, no throughput, no parallelism' stands. CORRECTION to the secondary cross-check: the entry states the HF config.json has 'decoder_sparse_step=1 and mlp_only_layers=[]'. I fetched it — decoder_sparse_step=1 is present, but mlp_only_layers is absent from this config entirely. All other config values verified (hidden 3584, moe_intermediate 2560, 64 experts, top-8, 28 layers, shared_expert_intermediate_size 20480). The inference that all 28 layers are MoE therefore rests on decoder_sparse_step alone.
[confirmed] Qwen3-235B-A22B (pre-training run) [deepseek-qwen]: arXiv:2505.09388 verified: 235B/22B, '128 total experts with 8 activated experts per token', 'Qwen3-MoE design excludes shared experts', Table 2 row 'Qwen3-235B-A22B | 94 | 64 / 4 | 128 / 8 | 128K', 36T tokens across 119 languages, S1 >30T at 4,096, S2 ~5T at 4,096, S3 32,768. Negative findings verified by keyword count: zero MFU, zero TFLOPS, zero A100/H100/H800/Ascend/Megatron/cluster; the only substantive GPU-hours mention is the relative distillation-vs-RL comparison, exactly as the entry says. config.json cross-check confirmed: hidden 4096, intermediate 12288, moe_intermediate 1536, 128/8, 94 layers, 64/4 heads — consistent with Table 2 on every overlapping field.
[confirmed] Qwen3-30B-A3B (pre-training run) [deepseek-qwen]: Report claims verified: Table 2 row 'Qwen3-30B-A3B | 48 | 32 / 4 | 128 / 8 | 128K', 128 experts / 8 activated, no shared experts, 36T tokens. IMPORTANT: the entry explicitly flagged its parenthesised hidden=2048 and moe_intermediate=768 as UNVERIFIED and asked for a re-check. I fetched huggingface.co/Qwen/Qwen3-30B-A3B/raw/main/config.json — both are correct: hidden_size 2048, moe_intermediate_size 768, plus num_experts 128, num_experts_per_tok 8, num_hidden_layers 48, decoder_sparse_step 1, mlp_only_layers [] (so all 48 layers are MoE). The caveat can be lifted, keeping the release-artifact-not-report provenance label.
[confirmed] Qwen3-Coder-480B-A35B-Instruct [deepseek-qwen]: Provenance warning is accurate — no paper exists. All three first-party artifacts verified. Model card: '480B in total and 35B activated', 62 layers, 96 Q / 8 KV, 160 experts, 8 activated, '262,144 natively'. config.json verified field-for-field: hidden 6144, intermediate 8192, moe_intermediate 2560, num_experts 160, top-8, 62 layers, shared_expert_intermediate_size 0, decoder_sparse_step 1, mlp_only_layers [], max_position 262144, vocab 151936. Blog verified verbatim for the 7.5T/70%-code line and the 20,000-environments line, and I confirmed the blog gives no GPU type, GPU count, MFU, TFLOPS or GPU-hours — so 'throughput NOT REPORTED' is right.
[confirmed] Qwen3-Coder-Next (80B-A3B) [deepseek-qwen]: I checked the future-dated arXiv ID first because it was the highest fabrication risk: arXiv:2603.00729 is a real paper titled 'Qwen3-Coder-Next Technical Report' whose abstract matches. Verified: 80B total / 3B active per forward pass, 'expand the training context length from 32,768 tokens to 262,144 tokens', 'repository-level data is expanded to approximately 600B tokens', MegaFlow on Alibaba Cloud Kubernetes. Negative findings verified by keyword count on the full text: zero hits for 'hidden size', 'MFU', 'GPU', 'top-k' and 'layers'. The seven 'experts' hits are all about distilled expert *models*, not MoE expert counts — so the entry's blanket 'NOT STATED in the report' for architecture is correct.
[confirmed] Mixtral 8x7B [western-moe]: arXiv:2401.04088 verified. Table 1 exact: dim 4096, n_layers 32, head_dim 128, hidden_dim 14336, n_heads 32, n_kv_heads 8, context_len 32768, vocab_size 32000, num_experts 8, top_k_experts 2. '47B parameters, but only uses 13B active parameters during inference' and 'trained with a context size of 32k tokens' verified verbatim, as is 'we replace all FFN sub-blocks by MoE layers while GShard replaces every other block'. I reproduced the negative grep: zero hits across GPU, H100, A100, MFU, GPU-hour and training-token terms. config.json cross-check matches Table 1 exactly.
[confirmed] Mixtral 8x22B [western-moe]: Correctly handled as a no-paper model. Mistral announcement verified verbatim: 'uses only 39B active parameters out of 141B' and 'Its 64K tokens context window ...'. I also confirmed the negative: the announcement page gives no training token count, no GPU type/count, no MFU/TFLOPS and no hidden/layer dimensions. config.json verified field-for-field: hidden_size 6144, intermediate_size 16384, num_hidden_layers 56, num_attention_heads 48, num_key_value_heads 8, num_local_experts 8, num_experts_per_tok 2, vocab_size 32000, max_position_embeddings 65536.
[confirmed] Grok-1 [western-moe]: Both artifacts verified. xAI page: '314 billion parameter Mixture-of-Experts model', '25% of the weights active on a given token', 'custom training stack on top of JAX and Rust in October 2023' — and I confirmed the page prints no active-parameter figure in billions, no GPU type/count, no MFU/TFLOPS/tokens-per-second and no token count. The entry's decision to treat the active count as unstated rather than printing 78.5B is the correct call. run.py verified line-for-line (vocab 128*1024, sequence_len 8192, emb_size 48*128, widening_factor 8, key_size 128, 48 Q / 8 KV heads, 64 layers, 8 experts, 2 selected, local_mesh_config (1,8)). model.py ffn_size verified verbatim, and I executed the formula: it returns exactly 32768, so the derived MoE intermediate width is right.
[confirmed] DBRX (dbrx-base) [western-moe]: Databricks blog verified verbatim for 132B/36B, '16 experts and chooses 4', 12T tokens, 32k max context, '3072 NVIDIA H100s connected by 3.2Tbps Infiniband', the three-month sentence covering pretraining+post-training+evaluation+red-teaming+refining, and 150 tok/s/user. I independently confirmed all three inaccessibility claims: the blog itself provides no d_model, hidden size or layer count; huggingface.co/databricks/dbrx-base/raw/main/config.json returns HTTP 401; the databricks/dbrx GitHub raw path returns 404. The refusal to import the widely-circulated d_model=6144 / ffn=10752 / 40-layer figures from third-party mirrors is exactly right, as is treating the three-month figure as unusable for a throughput derivation.
[confirmed] Llama 4 Scout (17Bx16E) [western-moe]: Model card verified: 'Llama 4 Scout (17Bx16E)', 17B (Activated) / 109B (Total), 10M context, ~40T tokens; 'cumulative of 7.38M GPU hours ... on H100-80GB (TDP of 700W)'; row 'Llama 4 Scout | 5.0M | 700 | 1,354 | 0'; 'Llama 4 Scout was pretrained on ~40 trillion tokens'. moe.py line 156 verified verbatim: 'self.shared_expert = FeedForward(dim, hidden_dim, do_reduce=False)'. Derived 2,222 tokens/s/GPU reproduces exactly (40e12 / 1.8e10 GPU-seconds), and the caveat that the denominator is total-GPU-time rather than pretraining-only is appropriate. The refusal to carry the blog's 32K-GPU count or 390 TFLOPs/GPU across from Behemoth is correct — both are Behemoth-only.
[confirmed] Llama 4 Maverick (17Bx128E) [western-moe]: Blog quote verified verbatim in full, including 'alternating dense and mixture-of-experts (MoE) layers', '128 routed experts and a shared expert', and 'Each token is sent to the shared expert and also to one of the 128 routed experts' — which is what supports the unusual top_k=1-plus-shared reading. Card verified: 17Bx128E, 17B Activated / 400B Total, 1M context, ~22T tokens, row 'Llama 4 Maverick | 2.38M | 700 | 645 | 0'. Derived 2,568 tokens/s/GPU reproduces exactly (22e12 / 8.568e9). The extra caveat that Behemoth codistillation forward passes may sit inside the GPU-hour denominator is well founded and correctly stated.
[confirmed] Llama 4 Behemoth (preview, still training) [western-moe]: Blog verified: '288 billion active parameter model with 16 experts', 'nearly two trillion total parameters' (no exact figure), 'while pre-training our Llama 4 Behemoth model using FP8 and 32K GPUs, we achieved 390 TFLOPs/GPU', 'more than 30 trillion tokens' as the overall Llama 4 mixture, and 'it is still training'. The throughput handling is exemplary and directly addresses the metric-confusion risk in the brief: 390 TFLOPs/GPU is FP8 and Behemoth-only, Meta frames it as evidence of 'high model FLOPs utilization' without ever printing an MFU percentage, and because the GPU type for that run is unstated you cannot divide by a peak to back one out. The instruction not to do so is correct.
[confirmed] OLMoE-1B-7B [western-moe]: arXiv:2409.02060 verified. 5.1T headline / 5.133T actual (1.3 epochs), 6.9B total / 1.3B active, hyperparameter table values, 'no shared expert'. Appendix B hardware verified verbatim: 256 H100 for ~10 days, NVLink across GPUs and InfiniBand across nodes. Crucially the throughput attribution is right: '23,600 tokens per second per GPU for the MoE vs. 37,500 for dense' sits in the Figure 4 discussion whose caption states 'each on 128 H100 GPUs' — so it is the 128-GPU controlled ablation, not the 256-GPU production run, exactly as flagged. EC-vs-TC 29,400 vs 24,400 verified. No MFU or HFU anywhere. Treating the 23,207 back-calculation as a sanity check rather than a reportable number is the right call given 'approximately 10 days'.
[confirmed] Phi-3.5-MoE (16x3.8B) [western-moe]: Model card verified verbatim: 16x3.8B with 6.6B active using 2 experts, vocabulary 32,064, 128K context, 'GPUs: 512 H100-80G', 'Training time: 23 days', 'Training data: 4.9T tokens'. config.json verified: hidden 4096, intermediate 6400, 32 layers, 16 local experts, top-2, 32/8 heads, vocab 32064. Derived 4,816 tokens/s/GPU reproduces exactly (4.9e12 / 1,987,200 s / 512), and the caveat that the card never says the 23 days is pure pretraining wall-clock is appropriate. The hedge against importing GRIN's no-expert-parallelism recipe into this run is also correct — they are different runs.
[confirmed] GRIN MoE (16x3.8B) [western-moe]: arXiv:2409.12136 verified. 42B/6.6B, top-2 of 16, Table 1 (n_embed 4096, inner_dim 6400, 32 layers, moe_mod 'every layer', n_position 4096, vocab 32064), 4T pretraining plus 24B SFT and 1.4B DPO, both parallelism quotes verbatim, and 'With 272 H100 GPUs ...' as a projection. The key throughput flag is confirmed: Table 3's caption is 'Training Throughput of Dense and MoE on 64 H100 gpus' and its column header reads literally 'Throughput Per GPU' with no unit printed anywhere; values 34222 / 27962 / 81.71% and 8176 / 7077 / 86.56% all match. Declining to assume tokens/s and declining to compute an MFU without a FLOPs-per-token term are both correct.
[confirmed] GLaM 64B/64E [western-moe]: arXiv:2112.06905 verified. 1.2T total with 96.6B (8%) activated, 64 experts, Table 4 row M=8,192 and H=32,768 (dense 137B comparator H=65,536), 'largest GLaM 64B/64E model was trained on 1,024 Cloud TPU-V4 chips', max sequence 1024 with 'up to 1 million tokens per batch', vocab 256K, '1,024 TPU-v4 chips for 574 hours (with 280B tokens)', 213 MWh, 456 MWh for the full 600B-token run, '326W measured system power per TPU-v4 chip', every-other-layer MoE. Derived 132 tokens/s/chip reproduces (280e9 / 2,066,400 s / 1024). No MFU/HFU/TFLOPS is reported, and the explicit refusal to convert — because the 180 GFLOPs/token figure is an inference number with no stated forward/backward multiplier — is exactly the right restraint.
[confirmed] Switch-C [western-moe]: arXiv:2101.03961 Table 9 verified exactly: 'Switch-C | 1571B | 890B | 2080 | (no GEGLU) | 6144 | 64 | 32' and 'Switch-C | 1 | 15 | 2048 | -1.096 | -1.043'. 'The Switch-C model is designed using only expert-parallelism, and no model-parallelism' and the 4x-faster-to-fixed-perplexity claim verified verbatim. The 2^20-tokens-per-batch x 550k-steps = 576B protocol sentence exists as quoted, and the entry is right that it is not attached to Switch-C. Table 1 examples/sec verified (T5-Base 1600, T5-Large 470, Switch-Base 860), as is '32 TPUv3 cores' for those benchmarks. Reporting active params as unstated (890B FLOPs/seq instead) is correct.
[confirmed] Switch-XXL [western-moe]: arXiv:2101.03961 Table 9 verified exactly: 'Switch-XXL | 395B | 6.3T | 4096 | GEGLU | 10240 | 64 | 64' and 'Switch-XXL | 1/2 | 24 | 64 | -1.086 | -1.008'. The 503B-token partially-pretrained checkpoint and 'we do not pre-train for a full 1M steps' verified, as is the FLOP-matched-to-T5-XXL framing with added model-parallelism communication cost. No throughput reported for this model — correct.
[confirmed] GShard MoE(2048E, 36L) 600B [western-moe]: arXiv:2006.16668 verified. 600B parameters on 2048 TPU v3 devices for 4 days, 1 trillion (10^12) tokens, Table 3 row one: 2048 cores | 0.72 steps/sec | 4M tokens batch | 22.4 TPU core-years | 4.0 days | 44.3 BLEU. Appendix A.2 dims verified (1024 model dim, 8192 FFN/MoE hidden, 16 heads, 128 key/value dim), every-other-layer MoE and top-2 gating verified. The per-operator peak-FLOPS statement verified verbatim: '>85% peak FLOPS' for feed-forward/projections vs '>30% peak FLOPS' for attention — correctly labelled as per-operator, not end-to-end. Derived 1,406 tokens/s/core reproduces (0.72 x 4e6 / 2048). Minor: the device/expert-tying sentence appears in the paper as 'We tied the number of devices used for training to the number of experts per MoE layer for simplicity'; the entry's wording differs slightly but the substance is right.
[confirmed] Llama 3.1 405B (DENSE contrast) [western-moe]: arXiv:2407.21783 verified. Table 3 (126 layers, model dimension 16,384, FFN 53,248, 128 heads, 8 KV, vocab 128,000, RoPE theta 500,000) and Table 4 rows verified, first row exactly '8,192 | 8 | 1 | 16 | 64 | 8,192 | 32 | 16M | 430 | 43%'. 15.6T tokens, 3.8e25 FLOP budget, up to 16K H100 at 700W/80GB HBM3 on Grand Teton with NVLink intra-node, RoCE on Arista 7800 + Minipack2 at 400 Gbps for 405B and Quantum2 InfiniBand for smaller models — all verified. The throughput handling is correct and unusually clean: the paper labels the metric explicitly as 'BF16 Model FLOPs Utilization (MFU ...) of 38-43%' and gives TFLOPs/GPU alongside, and the entry keeps them distinct.
[confirmed] MiniMax-Text-01 (MiniMax-01) [chinese-large]: arXiv:2501.08313 verified. 456B/45.9B, 80 layers, hidden 6144, 32 experts with top-2, per-expert FFN hidden 9216, 64 heads at dim 128 with GQA group 8, 'number of H800 GPUs ranges from 1500 to 2500', sequence length 8192, batch 16M -> 32M at 69B -> 64M at 790B -> 128M at 4.7T, LR phases 7.2T / 3.2T / 1T, and the world_size = PP x EDP x ETP x EP constraint with no degrees given. The single most important flag is confirmed: the >75% MFU appears in a sentence about 'CUDA kernels tailored for lightning attention inference, achieving over 75% Model Flops Utilization (MFU) ... end-to-end on the Nvidia H20' — it is an inference figure on H20, not a training figure on the H800 cluster. Refusing to report it as training throughput is correct.
[confirmed] Kimi K2 [chinese-large]: arXiv:2507.20534 verified. 1.04T total / 32.6B activated (abstract says 32B), hidden 7168, MoE expert hidden 2048, 384 experts, top-8, 1 shared, 61 layers with 1 dense layer, 64 heads, sparsity 48. Cluster verified verbatim: H800 nodes with 2 TB RAM, NVLink/NVSwitch intra-node, 8x400 Gbps RoCE inter-node. 'any number of nodes that is a multiple of 32', 16-way PP with virtual stages + 16-way EP + ZeRO-1, 'EP = 16', and ~6 TB 'distributed over a model-parallel group of 256 GPUs' — so the entry is right that no cluster size is stated. 15.5T tokens, 4,096 context, 67M constant global batch. I checked the no-throughput claim carefully: a case-insensitive scan returns four apparent MFU hits, all of which are the substring inside the word 'Harmful' in the safety section. No MFU, HFU, TFLOPS or tokens/second exists in the report.
[confirmed] Kimi k1.5 [chinese-large]: arXiv:2501.12599 verified, including the deliberate withholding. Appendix B.3 quote verified verbatim ('Specific details regarding model architecture scaling experiments lie beyond the scope of this report ...'), and the long-context activation to 131,072 tokens verified. I ran a keyword scan for MFU, TFLOPS, H800, A100, H100 and 'total parameters' and got zero hits across all six — so the blanket NOT STATED for total/active params, widths, expert counts, accelerator, count, throughput and parallelism degrees is fully supported. The warning not to carry K2's numbers across, and the note that the report does not even state dense vs MoE, are both correct.
[confirmed] GLM-4.5 [chinese-large]: arXiv:2508.06471 Table 1 verified column-for-column: 355B total, 32B activated, 3 dense layers, 89 MoE layers, 1 MTP layer, hidden 5120, dense intermediate 12288, MoE intermediate 1536, head dim 128, 96 attention heads, 8 KV heads, 160 experts, 8 active per token, 1 shared, QK-Norm yes. 23T tokens verified in the abstract; batch warmup 16M -> 64M over the first 500B tokens and sequence 4,096 extended to 32,768 and 131,072 verified verbatim. Negative finding verified: a scan for MFU/TFLOPS/H800/H100/GPU-hour returns one hit, which is an unrelated safety-benchmark sentence — no hardware section, no accelerator, no throughput.
[confirmed] GLM-4.5-Air [chinese-large]: arXiv:2508.06471 Table 1 Air column verified: 106B total, 12B activated, 1 dense layer, 45 MoE layers, 1 MTP layer, hidden 4096, dense intermediate 10944, MoE intermediate 1408, head dim 128, 96 heads, 8 KV heads, 128 experts, 8 active, 1 shared, QK-Norm no. The entry's care in not splitting out an Air-specific token count — the 23T / 4,096-seq / 16M->64M recipe is described for the series without disaggregation — is correct.
[confirmed] Hunyuan-Large [chinese-large]: arXiv:2411.02265 Table 1 verified: caption confirms 389B total and 52B activated with 1 shared plus 1 specialized expert activated per token; rows 64 layers, 80 attention heads, 8 KV heads, 1 shared expert, 16 specialized experts, 1 activated specialized, 7T trained tokens, SwiGLU, vocab 128K, hidden size 6,400. 'nearly 1.5T tokens of high-quality and diverse synthetic data' verified. Negative findings verified: a scan for GPU/NPU/cluster/MFU/TFLOPS/'intermediate size' returns essentially nothing — the report has no systems section, names no accelerator, and gives no expert intermediate width. Worth noting for the author: MegaScale-MoE's Table 2 independently lists Hunyuan-Large h_ffn = 18304, a third-party reconstruction the entry correctly did not import here.
[confirmed] Step-2 (StepFun) [chinese-large]: The negative finding is verified against the primary source it cites. In Step-3 (arXiv:2507.19427) Step-2 is mentioned exactly once, in the sentence quoted, and reference [20] resolves to 'StepFun. Step 2. https://platform.stepfun.com/docs/llm/text, 2025' — an API documentation page, not a technical report. Reporting every field as NO PRIMARY SOURCE FOUND and warning against substituting Step-3's numbers is the correct handling.
[confirmed] Step-3 (StepFun) [chinese-large]: arXiv:2507.19427 verified. 61 Transformer layers with hidden dimension 7168, MFA with 64 query heads at head dimension 256 and query down-projection to rank 2048, 1 shared expert, 'MoE layers are applied to all FFNs except the first four and the last layer', 316B parameters with 38B activated per token, plus a 5B vision encoder for 321B VLM total. The throughput labelling is correct and important: 'a decoding throughput of up to 4,039 tokens per second per GPU under 50ms TPOT SLA (4K context, FP8, no MTP)' vs DeepSeek-V3's 2,324 is a decoding figure on Hopper, and the entry does not present it as training throughput. Routed-expert count and top-k are genuinely absent — correct.
[corrected] Skywork-MoE [chinese-large]: Almost everything verified in arXiv:2406.06563: 146B total / 22B activated; Table 2 (vocab 65,536, hidden 4,608, FFN 12,288, head dim 128, 36 heads, 52 layers, 16 total experts, 2 routed, MoE layer frequency 1, native seq 8192); Appendix B.3 verbatim (192 NVIDIA-HGX-A800 nodes = 1536 A800-80G SXM, 400 GB/s NVLink intra-node, 800 Gb/s RoCE inter-node, 12-way PP, 4-way tensor-expert via EDP, 32-way DP with ZeRO-1); and '38% Model Floating-point Utilization (MFU) on the cluster and a throughput of 690 tokens per GPU per second'. 12 x 4 x 32 = 1536 is consistent. CORRECTION — MISATTRIBUTED QUOTE: the entry supports 'NO shared experts (explicitly rejected)' with the quote 'The computational efficiency of the Shared Expert is relatively low ... Thus in our model, we take the classical routing strategy without any shared experts.' That sentence does not appear in the Skywork-MoE paper — the string 'shared expert' occurs nowhere in it. I located the sentence verbatim in Yuan 2.0-M32 (arXiv:2405.17976, PDF p.4), which is the sibling row that also quotes it. Drop the quote here and downgrade the claim to: Table 2 lists no shared-expert row, so the architecture appears to have none, but the paper never states this.
[confirmed] Yuan 2.0-M32 [chinese-large]: arXiv:2405.17976 PDF extracted and verified. Abstract: 32 experts of which 2 active, 2000B tokens from scratch, 'training computation consumption is only 9.25% of a dense model at the same parameter scale', 3.7B active of 40B total, 7.4 GFlops forward per token; 22.2 GFlops per token for fine-tuning verified later in the paper. 'we set M=2, N=32, d=2048' verified verbatim. Parallelism sentence verified verbatim ('data parallelism and pipeline parallelism, however neither tensor parallelism nor optimizer parallelism is used'). Appendix A verified: sequence length 4096 / 16384 and global batch 1536 / 1152, with units genuinely unspecified as the entry notes. The shared-expert rejection quote is authentically from this paper — it follows an ablation showing the Shared Expert router reached identical test loss with 7.35% more training time. No MFU/HFU/TFLOPS/tokens-per-second anywhere, correct.
[confirmed] TeleChat2 / TeleChat2.5 / T1 - 115B (DENSE, Ascend) [chinese-large]: arXiv:2507.18013 verified. Table 1 115B row (96 layers, d_model 8192, d_ffn 40960, 64 heads, 8 KV, vocab 131072), 10T tokens with 8K first-stage sequence, migration from 640 NVIDIA A100 to ctyun's Shanghai centre, 'Atlas 900 A2 cluster with 8k Ascend NPUs', 8 HCCS-connected NPUs per node, 200GE RoCE two-layer Clos with NSLB. Throughput correctly labelled: 'a Model FLOPs Utilization (MFU; Chowdhery et al. (2022)) of 33.8%-36.3%', and Table 3 rows verified exactly — 512 cards DP8/TP8/PP8/CP1/VPP1/BS128/8k/1M -> 36.3, and 4096 cards DP64/TP8/PP8/CP1/VPP2/BS512/8k/4M -> 33.8. The flagged Table 2 vs Table 3 batch-token discrepancy is real. One addition for completeness, not an error: the body text also reports 34.1% MFU on a 6144-NPU cluster with VPP=3 and 34.5% MFU for 128k-sequence training on 4096 NPUs with CP — figures outside Table 3 that the entry did not capture.
[confirmed] TeleChat3-MoE 1119B [chinese-large]: I verified the future-dated ID first: arXiv:2512.24157 'Training Report of TeleChat3-MoE' is a real paper with a matching abstract. Table 1 1119B row verified exactly: 1119B | 61 | 5,120 | 128 | 384 | 3072 | 8 | 1, with the caption's vocab 131,072 and 32K-128K context. There is genuinely no activated-parameter column, so 'NOT STATED' and the refusal to compute it are both correct. 'a trillion-parameter model on 8192 devices' verified. The relative-only throughput claims all verified verbatim: ~15% higher training throughput at EP=16, 25-30% from the firmware idle-mode fix on 4096 devices, 10-15% from CPU/kernel isolation at large scale, an additional 3-5% from IOMMU passthrough, and ~85% at the single-operator level for the fused GroupedMatMul-Reshape-Cast. Correctly reported as having no absolute MFU/TFLOPS/tokens-per-second.
[confirmed] TeleChat3-MoE 438B [chinese-large]: arXiv:2512.24157 Table 1 438B row verified exactly: 438B | 54 | 5,120 | 128 | 256 | 2048 | 8 | 1. The entry's careful framing of the 4,096-device figure is precisely right — the sentence is 'Taking 438B as an example, the tool performs multiple searches over the dimensions of DP, MP (tensor parallelism), PP, EP, and MB (micro-batch number) on a cluster with 4,096 devices and a global batch size of 16,384', i.e. an automatic-parallelization search example, not a stated production cluster size. The pipeline-balancing example (8 stages, 2 interleaved pipelines, 64 micro-batches, 45GB limit) is also in the paper.
[confirmed] TeleChat3-MoE 105B [chinese-large]: arXiv:2512.24157 Table 1 105B row verified exactly: 105B | 45 | 2,560 | 32 | 192 | 1536 | 4 | 1. The qualitative sparsity sentence ('top-4 to top-8 routing out of hundreds of routed experts plus only one shared experts') and 'the 105B model (trained efficiently on modest clusters)' both verified verbatim, supporting the NOT STATED accelerator count.
[confirmed] Pangu Pro MoE (72B-A16B, MoGE) [chinese-large]: arXiv:2505.21411 verified. Table 1 exactly: vocab 153376, hidden 5120, intermediate 1344, 40 query heads, 8 KV heads, head size 128, 48 layers, 64 routed experts, 8 activated, 4 shared, 16.50B activated, 71.99B total. 13 trillion tokens on 4K Ascend NPUs verified; Ascend 800T A2 with 256 TeraFLOPS FP16, 512 TeraOPS INT8 and 310W verified verbatim. The throughput handling is correct on the point that matters: the paper reports only 'a 35% relative increase in MFU', and Table 2 shows the baseline MFU as '-' with the optimized entry as 'increase by 35%' — so no absolute training MFU exists, exactly as stated. Parallelism TP8/EP2/CP1/PP5/VPP5 (baseline TP1/EP8/CP8/PP6/VPP4) and the 48+2 no-op layers verified. Inference figures 1148 -> 1528 tokens/s/card with MTP and 4828 tokens/s/card prefill verified and correctly kept separate from training.
[confirmed] Pangu Ultra MoE (718B) [chinese-large]: arXiv:2505.04519 verified. 718B/39B (Table 6), hidden 7680 and 61 layers and 256 routed experts (Table 2, Model 7), 1 shared expert with both shared and routed intermediate size 2048 and always 8 activated. MFU 30.0% with TPS 1.46M on 6K Ascend NPUs vs baseline 18.9% and 0.61M on 4K, and Table 5's cumulative +15.8% / +28.6% / +49.2% / +58.7% all verified verbatim. TP=8, PP=16, VPP=2, EP=4, MBS=2 verified in two places. Two caveats independently confirmed as accurate: (1) 'Ascend 910B platform' appears only as the target the simulator models, never as an explicit statement about the 6K-NPU production run; (2) the sequence length 8192 / global batch 64 / micro-batch 1 figures belong to a separate 4.2B six-layer 128-NPU calibration run with 12288 hidden and 4 experts — completely different from the 718B config, so the warning not to attribute them is well founded.
[confirmed] Pangu Ultra 135B (DENSE, Ascend) [chinese-large]: arXiv:2504.07866 verified. 135 billion parameters, hidden 12,288, SwiGLU FFN intermediate 28,672, 94 layers, 96 query and 8 KV heads — all verbatim. 13.2 trillion tokens verified. Cluster verified in full: 8,192 Ascend NPUs, 8 NPUs per node over HCCS full-mesh with 64GB per device, inter-node RDMA over Converged Ethernet at 200 Gbps. Parallelism verified: 128-way DP with ZeRO, 8-way TP, 8-way PP, 6-way virtual PP (and 128 x 8 x 8 = 8,192 is consistent). MFU explicitly labelled and correctly staged: 'approximately 43% MFU on an 8,192 NPU cluster as a baseline', then 'over 50%', then 'over 52% MFU, representing a 9% relative improvement'.
[confirmed] MegaScale-MoE - Internal-352B [systems-papers]: arXiv:2505.11432 verified. Table 2 row exact: 'Internal-352B | 60 | 4096 | 32 | 4 | 14336 | 32 | 3'. Table 3 verified value-for-value: MegaScale-MoE 272.9k (1.81x) / 498.6k (1.65x) / 740.1k (1.72x) / 963.8k (1.77x) / 1407.7k (1.88x) with iteration times 21.61 / 11.83 / 7.97 / 6.12 / 4.19 s and 1T-token days 42.41 / 23.21 / 15.64 / 12.01 / 8.22; Megatron-LM 151.1k ... 746.6k. The MFU sentence verified verbatim: 'the MFU (Model FLOPs Utilization) of MegaScale-MoE declines from 32.48% to 27.89%', with only those two endpoints in text. Sequence length 8,192, vocab 65,536, PP=15 for both systems, global batch fixed at 720, weak scaling 360->1,080 with Megatron degrading 2.74% vs MegaScale-MoE 0.2%, and Table 4 H800 at 989 TFLOPS / 80 GB / 3.4 TB/s / 400 GB/s NVLink — all verified. Correctly declining to recover the intermediate MFU points that exist only in figures.
[confirmed] MegaScale-MoE - open-source model configurations [systems-papers]: arXiv:2505.11432 Table 2 verified row-for-row for all five: Mixtral-8x7B 32/4096/32/4/14336/8/2; Mixtral-8x22B 56/6144/48/6/16384/8/2; Hunyuan-Large 64/6400/80/10/18304/16/1; Phi-3.5-MoE 32/4096/32/4/6400/16/2; DeepSeekMoE 28/2048/16/1/1408/64/6. The 32-GPU cross-GPU study verified ('DP size as four, the TP size as eight for Megatron-LM, and the SP and EP size as eight for MegaScale-MoE', 'up to 1.58x in MFU'), as is the 128-GPU selective-rematerialisation study and the single-node study with global batch 32 achieving '14.9%-32.9% higher MFU compared to TP+TP'. The entry is right that the absolute MFU values live only in Figures 13b/14/17 and are not recoverable as text; reporting them as unknown rather than eyeballing the plots is the correct call.
[confirmed] MegaScale - 175B GPT-style transformer (DENSE) [systems-papers]: arXiv:2402.15627 verified. Table 1 row '175B | 128 | 12288 | 96 | 8 | 8'. Table 2 verified value-for-value: MegaScale at batch 768 gives 65.3% (1.23x) / 63.5% (1.27x) / 61.3% (1.31x) / 59.0% (1.32x) and at batch 6144 gives 59.1% (1.21x) / 57.3% (1.19x) / 54.9% (1.26x) / 55.2% (1.34x), with tokens/s 49.0k / 95.1k / 136.7k / 176.9k and 531.9k / 1030.9k / 1315.6k / 1984.0k and aggregate PFlops/s 52.2 ... 2166.3; Megatron-LM baselines 53.0 / 49.9 / 46.7 / 44.7 and 48.7 / 47.8 / 43.3 / 41.2. Caption's '300B tokens' normalisation, sequence length 2,048, vocab 64,000, six interleaving stages, >10,000 NVIDIA Ampere GPUs, and the Broadcom Tomahawk 4 network description all verified. Metric is explicitly labelled MFU by the paper.
[confirmed] MegaScale - 530B GPT-style transformer (DENSE) [systems-papers]: arXiv:2402.15627 verified. Table 1 row '530B | 160 | 20480 | 105 | 8 | 35', three interleaving stages, and the '12,288 vs. 11,200' sentence explaining the differing maxima. The two text claims verified verbatim: MFU 'higher than Megatron-LM by up to 6.1%' and 'the MFU of Megatron-LM decreases by 1.6%'. Most importantly I searched the full text for 54.30, 54.10 and 2240 and found nothing — the per-scale 530B MFU values genuinely exist only inside Figure 9. The entry's explicit refusal to carry the third-party-circulated '54.30% at 2240 GPUs / 54.10% at 4480 / 54.30% at 11200' figures is correct and is exactly the discipline this task calls for.
[corrected] MoE Parallel Folding / Megatron-Core - Mixtral 8x22B [systems-papers]: All MFU values verified exactly in arXiv:2504.14960. Table 4: MCore 49.4 / 48.0 / 45.5 / 42.3; MCore w/ Folding 52.2 / 50.7 / 48.9 / 44.9; FSDP+EP 23.9 / 25.5 / 24.4 / 23.8; TP+EP+DP 40.4 / 39.1 / 36.0 / 36.2. Table 1/3 at 128 GPUs: Folding 49.3%, MCore 46.3%, TP+EP+DP 36.6%, FSDP+EP 23.4%, FSDP 4.3%. Table 2 per-GPU TFLOPS 458.3 / 487.7 / 575.1 / 631.7. Eos hardware verified (989.5 TFLOP/s peak half-precision, 450 GB/s NVLink, 400Gbps InfiniBand, BF16 measurements, up to 1024 GPUs) as is token-drop at capacity factor 1. The flagged 49.3%-vs-52.2% discrepancy at 128 GPUs is real and correctly transcribed, and the dual metric naming is real — 'Model Flops Utilization (MFU)' in the abstract vs 'Model TFLOPS Utilization (MFU)' in Sec 4.2. CORRECTION: the entry gives global batch 256 for the scaling experiments. Sec 4.3 states explicitly 'The global batch size is set to 1024 in the scaling experiments.' The 256 applies only to the Table 1 / Table 3 optimal-configuration comparison. The sibling scaling-laws row has this right; this row should be amended to 1024 for the Table 4 curve.
[confirmed] MoE Parallel Folding / Megatron-Core - Llama3-8x70B [systems-papers]: arXiv:2504.14960 verified. Table 4 rows exactly as transcribed, including the anomaly: MCore w/ Folding is printed as 128 / 43.7%, 512 / 42.7%, 1024 / 41.5% and FSDP+EP as 128 / 18.9%, 512 / 17.3%, 1024 / 17.1%, while MCore starts at 256 / 40.1%. Table 3 at 256 GPUs verified (FSDP OOM, FSDP+EP 19.6%, TP+EP+DP OOM, MCore 38.8%, Folding 41.6%) and the body text 'the MFU only drops from 43.7% to 41.5%' verified. The decision to transcribe the '128' label as printed while flagging it as a probable typo for 256 — rather than silently correcting it — is the right handling, and the supporting reasoning (Table 3 configures this model at 256) is sound. Upcycling description verified verbatim.
[confirmed] MoE Parallel Folding / Megatron-Core - Qwen2-57B-A14B [systems-papers]: arXiv:2504.14960 verified. Setup sentence verbatim: '64 experts and 8 active experts per token, totaling 57 billion parameters with 14 billion active parameters'. Table 4 full curve verified exactly: MCore w/ Folding 39.9 / 39.7 / 38.1 / 36.6 / 33.4; MCore 36.2 / 36.0 / 34.8 / 32.5 / 29.8; FSDP+EP 26.3 / 25.6 / 23.4 / 22.8 / 21.6; TP+EP+DP 22.4 / 20.9 / 19.8 / 19.7 / 17.9. Table 1/3 at 64 GPUs (Folding 39.0%, MCore 35.3%, TP+EP+DP 23.1%, FSDP+EP 25.4%, FSDP 9.9%) and the abstract's 39.0% verified, as is the long-context 'MFU only drops from 38.7% to 35.9% for Qwen-57B14A'. Note the same global-batch scoping point as the Mixtral row: the Table 4 sweep uses global batch 1024, not 256.
[confirmed] MoE Parallel Folding / Megatron-Core - Mixtral-8x22B-G8T8 [systems-papers]: arXiv:2504.14960 verified. Reparameterisation sentence verbatim ('64 experts and 8 active experts per token ... each expert possessing a hidden size that is one-eighth of the original model, by applying fine-grained upcycling'). Table 4 verified exactly: MCore w/ Folding 30.0 / 29.3 / 26.7 / 25.5; MCore 19.8 / 18,4 / 16.3 / 13.4; FSDP+EP 9.0 / 8.5 / 8.9 / 8.6; TP+EP+DP 8.6 / 8.5 / 8.5 / 8.1. The comma typo '18,4%' at 256 GPUs is genuinely in the paper and transcribing it as printed while noting it evidently means 18.4% is correct. Table 1/3 at 128 GPUs verified (Folding 28.8%, MCore 17.1%, TP+EP+DP 8.7%, FSDP+EP 9.0%, FSDP 2.2%). Same global-batch scoping caveat as the sibling rows.
[confirmed] Megatron-Core MoE technical report - DeepSeek-V3-685B [systems-papers]: I verified the future-dated ID first: arXiv:2603.07685 is real, and its abstract matches the quoted line verbatim. Table 11 verified row-for-row: GB300/256/4,096/MXFP8 -> 1233 TF and 4,730 tokens/s/GPU; GB200 MXFP8 -> 1048 and 4,020; GB200 BF16 -> 857 and 3,298; H100/1024/FP8-BLK -> 368 and 1,412. There is genuinely no MFU column, so 'NO MFU IS REPORTED for these runs' is correct. Table 20 configs verified verbatim including 'TP2 PP4 CP1 EP64 VPP4 MBS1 GBS8192' for the H100 row. The force-balanced-routing caveat, the 685B = 671B main + 14B MTP note, the 18x gap, hidden 7168 from the dispatch benchmark, and the 88%-of-short-context-MFU ratio all verified. Flagging that the reported ratio is not an absolute MFU is correct.
[confirmed] Megatron-Core MoE technical report - Qwen3-235B [systems-papers]: arXiv:2603.07685 Table 11 verified row-for-row: GB300/256/4,096/MXFP8 -> 974 TF and 6,583 tokens/s/GPU; GB200 MXFP8 -> 919 and 6,212; GB200 BF16 -> 750 and 5,100; H100/256/BF16 -> 320 and 2,132; GB300/128/131,072/MXFP8 -> 1,150 and 1,556. No MFU column — correct. Table 20 configs verified verbatim, and the flagged rendering ambiguity is real: the source literally prints 'VPP 0 6' and 'VPP 0 4', so transcribing as printed while reading them as VPP=6 and VPP=4 is the right handling. The 129%-of-short-context-MFU ratio verified. Treating the 22B active figure as only implied by the single use of the full name 'Qwen3-235B-A22B' is appropriately cautious.
[confirmed] Pangu Ultra MoE 718B [systems-papers]: arXiv:2505.04519 verified as for the chinese-large row: 718B/39B, hidden 7680, 61 layers, 256 routed + 1 shared, intermediate 2048 for both, always 8 activated, 6K Ascend NPUs, MFU 30.0% and TPS 1.46M vs baseline 18.9% and 0.61M on 4K, Table 5 cumulative +15.8/+28.6/+49.2/+58.7, TP8/PP16/VPP2/EP4/MBS2, and the 128-NPU 4.2B calibration run with TP8/EP4/DP16/PP1. Additionally verified the 'relative 10% improvement in MFU' from dynamic device-level load balancing. The entry's caveat that the 30.0%/6K vs 18.9%/4K pair differs in both software stack and cluster size — so it is not a clean MFU-vs-scale curve — is a fair and correct reading.
[confirmed] DeepSpeed-MoE - 1.3B+MoE-128 (52B) [systems-papers]: arXiv:2201.05596 verified. 'there are in total 12 MoE layers for both 350M+MoE-128 and 1.3B+MoE-128', 'only the top-1 expert is selected', 'pre-trained ... on 128 Ampere A100 GPUs (Azure ND A100 instances)', 'we use 300B tokens to train both the dense model and the MoE models'. Table 1 column verified (24 layers, hidden 2048, 16 heads, 128 experts per layer, 52B parameters, 2K context, 300B tokens) and Table 3's metric is indeed 'Training samples per sec' with 372 and a 5x gain. Correctly identifying the metric as samples/sec rather than tokens/s, and declining to convert despite the 2K sequence length being available, is appropriate restraint.
[confirmed] DeepSpeed-MoE - 6.7B dense baseline [systems-papers]: arXiv:2201.05596 verified: '6.7B (32 layers, 4096 hidden size, 32 attention heads)', Table 1 column (6.7B parameters, 2K context, 300B tokens), Table 3 '6.7B dense | 70 | 1x' in training samples per second on 128 A100. No MFU or TFLOPS reported — correct.
[confirmed] Megatron-LM PTD-P (Narayanan et al. 2021) - GPT weak-scaling suite [systems-papers]: This row required going to the PDF because the arXiv HTML renders Table 1 as an image; I did that and the transcription matches row-for-row across all ten configurations: 1.7/24/2304/24/1/1/32/512/137/44%/4.4 through 1008.0/160/25600/128/8/64/3072/3072/163/52%/502.0, with every intermediate row matching too. Vocab 51,200, sequence 2048, A100 16-bit peak 312 teraFLOP/s, 140 teraFLOP/s per GPU on 1024 GPUs at batch 1536, the 450-billion-token assumption and 84-day estimate for the 1T model all verified. The load-bearing metric caveat is verified verbatim: 'equation (3) assumes activation recomputation and takes into account the floating-point operations associated with the extra forward pass.' The entry's conclusion that these percentages are recomputation-inclusive and therefore HFU-flavoured, not MFU, and must not be compared directly against MegaScale/MegaScale-MoE MFU figures, is correct and is the single most valuable metric-hygiene flag in the systems set.
[confirmed] Tutel - single MoE layer scaling microbenchmark [systems-papers]: arXiv:2206.03382 verified. Speedups 4.96x / 3.11x / 5.75x at 16 / 128 / 2,048 A100 GPUs verified verbatim in both abstract and body. Testbed verified verbatim (Azure Standard_ND96amsr_A100_v4, 8x A100 SXM 80GB, 8x 200 Gbps HDR InfiniBand, 3rd-gen NVLink/NVSwitch intra-VM, 1,600 Gbps non-blocking InfiniBand with adaptive routing across VMs, up to 2,048 GPUs = 256 VMs). Microbenchmark settings verified: tokens/step = 16384, f = 1, D = 2048, H = 2048, E_g = 2, top-k = 2, adaptive r = 1. Component gains (3.52x kernels at 16 GPUs vs 1.04x at 2,048; 4.25x 2DH All-to-All at 2,048) verified. Correctly reported as having no MFU/TFLOPS/tokens-per-second — the metric is step time and ratios.
[confirmed] Tutel - SwinV2-MoE [systems-papers]: arXiv:2206.03382 verified. 'Tutel accelerates SwinV2-MoE, achieving up to 1.55x and 2.11x speedup in training and inference over Fairseq' verified verbatim, as is the architecture sentence ('every other feed-forward layer replaced by an MoE layer except for the first two network stages ... SwinV2-B ... default hyper-parameters are: E = 32, top-k = 1, and f = 1.0') and the 32-GPU note for the 8- and 16-expert configurations. Metric is images/sec for training and inference on ImageNet-22K at 192x192 — correctly identified, with no MFU/TFLOPS/tokens-per-second claimed.
[confirmed] MegaBlocks - dMoE models MoE-XS / MoE-Small / MoE-Medium [systems-papers]: arXiv:2211.15841 verified. Table 1 verified (XS 512/6/46M; Small 768/12/125M; Medium 1024/24/356M; Large 1536/24/760M; XL 2048/24/1316M) with the caption's 'All models use ffn_hidden_size=4 x hidden_size'. Training setup verified verbatim: 'trained all models on 8 A100 SXM4 80GB GPUs using 8-way expert model parallelism for MoE layers and data parallelism for all other layers', batch 512 sequences with gradient accumulation, 10B tokens at sequence length 1024, vocab 51200, head size 64, 64-expert top-1 MoE layers. Speedups verified verbatim: 1.38x / 2.0x / 4.35x vs Tutel and 1.8x-2.4x vs Megatron-LM dense. Correctly reported as giving no MFU/HFU/TFLOPS/tokens-per-second — only end-to-end times and ratios.
[confirmed] HierMoE - scaled-down DeepSeek-V3 and Qwen3-30B-A3B [systems-papers]: arXiv:2508.09591 verified. Abstract speedups verbatim: 1.55x to 3.32x faster communication and 1.18x to 1.27x faster end-to-end on a 32-GPU cluster. Testbed verified (four nodes x eight NVIDIA RTX A6000-48G @1.46GHz, Table III's 112.5GB/s NVLink and Mellanox MT28908 @ 200Gb/s). The crucial surrogate caveat verified verbatim: 'Due to the GPU memory constraints of our testbed, we configure the hidden dimension and model dimension to be half of the original DeepSeek-V3 with 6 layers. For Qwen3-30B-A3B, we use 32 layers.' Micro batch 1, sequence length 1024, EP degree 32 verified. Flagging that these are not the real models, and that the hardware is prosumer rather than datacentre, is correct and important.
[confirmed] Lina - Transformer-XL / BERT2GPT2 / GPT-2 converted to MoE [systems-papers]: arXiv:2210.17223 verified. Abstract verbatim: 'up to 16 Ampere A100 GPUs with 40GB memory and 100Gbps InfiniBand ... accelerates all-to-all by at least 2.21x, and achieves on average 1.57x speedup in overall training step time compared to state-of-the-art system DeepSpeed. The median and 95%ile inference time is reduced by 1.45x and 1.63x.' Testbed verified ('four worker nodes. Each node has 4 Ampere A100 GPUs with 40GB memory and is equipped with 100Gbps InfiniBand'). Model list and expert sweep verified, as is 'top-2 gating in training and top-1 gating in inference'. The motivating measurements (all-to-all averaging 34.1% of step time, 3.7% average GPU SM efficiency during all-to-all) verified verbatim. No MFU/TFLOPS/tokens-per-second — correct.
[confirmed] SmartMoE - GPT-MoE and Swin-MoE [systems-papers]: USENIX ATC '23 PDF retrieved and verified. Table 2 exact: blinky 8x NVIDIA V100 PCIe / 32 / 50Gb/s; pinky 4x NVIDIA V100 SXM / 64 / 100Gb/s; inky 8x NVIDIA A100 SXM / 32 / 200Gb/s. Table 3 exact: GPT-MoE NLP batch 256/512, 4.5/7.3/9.9/14.0 billion params, capacity factors 1.2/2.4/4.8/+inf; Swin-MoE CV batch 4096, 0.54/1.0 billion. Speedups verified verbatim: 'on average 1.53x speedup on inky cluster, and ... on average 1.17x speedup on pinky cluster' with a maximum of 1.88x, up to 64 GPUs, GShard gate. The entry's uncertainty about which cluster each figure column belongs to is honest and the underlying figures are right. Minor: the abstract attributes the 1.88x specifically to 'the state-of-the-art MoE model training system FasterMoE' (named, singular); the entry's paraphrase says 'systems' but its baseline description is correct.
[confirmed] FSMoE - GPT2-XL-based and Mixtral-based MoE models [systems-papers]: arXiv:2501.10714 verified. Abstract verbatim, including 'up to a 1.42x speedup' across routing functions and 'outperforms the state-of-the-art MoE training systems (DeepSpeed-MoE and Tutel) by 1.18x-1.22x on 1458 MoE layers and 1.19x-3.01x on real-world MoE models based on GPT-2 and Mixtral'. The 48-GPU and 32-GPU clusters verified, Table 3 server configurations verified (Testbed A 8x Nvidia RTXA6000 @1.46GHz; Testbed B 4x Nvidia RTX2080Ti @1.35GHz) — consumer/prosumer parts, correctly flagged. 'communication overhead typically contributes over 50% to the overall training' verified. Metric is iteration time and ratios, with no MFU/TFLOPS/tokens-per-second — correct.
[confirmed] Comet - Mixtral-8x7B, Qwen2-MoE, Phi3.5-MoE [systems-papers]: arXiv:2502.19811 verified. Abstract verbatim: 1.96x single MoE layer, 1.71x end-to-end on average, 'adopted in the production environment of clusters with ten-thousand-scale of GPUs, achieving savings of millions of GPU hours'. Nvidia H800 and L20 clusters verified, and the Figure 1 caption confirms the breakdown is on 8 H800 GPUs using Megatron-LM. The parameter sentence verified verbatim: 'the Mixtral-8x7B model comprises 45 billion parameters in total, while only 14 billion parameters are active during runtime'. Worth a footnote for the author: those 45B/14B figures conflict with Mixtral's own paper (47B total / 13B active); the entry correctly attributes them to Comet rather than presenting them as Mixtral's spec. No MFU/TFLOPS/tokens-per-second — correct.
[confirmed] MixNet - optical-electrical reconfigurable fabric [systems-papers]: arXiv:2501.03905 verified. Abstract verbatim, including the 32-A100 prototype with in-training topology reconfiguration and the 1.2x-1.5x / 1.9x-2.3x networking-cost-efficiency gains at 100 Gbps / 400 Gbps versus a non-blocking fat-tree. Also verified: 1.4x-1.5x (2.3x-2.4x) versus Rail-optimized, up to 2.5x versus TopoOpt, scaling to 30K+ GPUs, 1.3x augmentation of NVL72, and the prototype hardware (32 Nvidia A100 GPUs, 16 Mellanox NICs, a Polatis millisecond OCS, an Ethernet switch). Correctly reported as having no MFU/TFLOPS/tokens-per-second — the metric is performance per dollar, and the entry says so plainly.
[confirmed] Ling-Plus / Ling-Lite (Ant Group) [systems-papers]: arXiv:2503.05139 verified. Ling-Lite 16.8B/2.75B and Ling-Plus 290B/28.8B verbatim; the flagged title-vs-abstract mismatch is real (the conclusion also says 'a 300B MoE LLM'). 9 trillion tokens split 1T Chinese / 5.5T English / 2.5T code verified. Table 1's anonymised device rows verified (A: 370 TFLOPS / 64 GB / 7 RMB-h / no FP8, and the rest follow) — the paper genuinely never names the vendors. The cost figures verified verbatim: ~6.35 million RMB per trillion tokens on device D versus ~5.08 million on lower-spec hardware, nearly 20% savings. Correctly reported as having no MFU/HFU/TFLOPS/tokens-per-second anywhere. One omission worth closing: the entry says global batch is NOT STATED, but the paper does give a batch warmup from 2,560 to a maximum of 8,960 with sequence length fixed at 4K — the sibling scaling-laws row captures this.
[unsourced] FasterMoE (PPoPP '22) [systems-papers]: No accessible primary source, and the entry handles this correctly. It reports every field as UNKNOWN, quarantines the circulating ACM-abstract paraphrase (the 'johnny' 16-GPU V100 cluster, 'up to 64 GPUs', and the '1.37X - 17.87X speedup' range) as unverified, and states explicitly that it must not be cited. No number here is usable in the paper without obtaining the PDF through an institutional subscription. Note that SmartMoE, which I did verify, names FasterMoE as its baseline and reports beating it by up to 1.88x — that is a citable secondary datapoint about FasterMoE's performance envelope if one is needed.
[unsourced] Janus (SIGCOMM '23) [systems-papers]: No accessible primary source, correctly handled. All fields UNKNOWN, and the paraphrased '16x traffic reduction / 2.06x speedup / 32-A100 cluster' figures are explicitly quarantined as uncitable. The one usable statement is the secondary characterisation from MegaScale-MoE's related-work section, which I did read in the primary source and which the entry quotes accurately. Do not promote any Janus number into the paper without the PDF.
[unsourced] ScheMoE (EuroSys '24) [systems-papers]: No accessible primary source, correctly handled. All fields UNKNOWN and the '9%-30% over Tutel and FasterMoE' paraphrase explicitly marked do-not-cite. The entry's suggested substitutes are sound and I verified both: FSMoE (arXiv:2501.10714) is open, from the same group, and covers the same design lineage; and HierMoE (arXiv:2508.09591) is open and refers to 'PipeA2A in ScheMoE'.
[confirmed] Chinchilla 70B [scaling-laws]: arXiv:2203.15556 verified. Table 4 Chinchilla row: 80 layers, 64 heads, key/value size 128, d_model 8,192, max LR 1e-4, batch 1.5M -> 3M tokens, with the caption confirming 'The feed-forward size is always set to 4 x d_model' and that the batch size doubles midway. 70B parameters on 1.4 trillion tokens verified in three places. 'All models in this analysis have been trained on TPUv3/TPUv4 with JAX and Haiku' verified verbatim — so accelerator type is stated but the mix and count are not, exactly as reported. Correctly reported as giving no MFU, HFU, TFLOPS or tokens/s of any kind.
[confirmed] PaLM 540B [scaling-laws]: arXiv:2204.02311 verified, and this is the cleanest MFU/HFU separation in the set. 'very high efficiency of 46.2% in model FLOPs utilization ... and 57.8% in hardware FLOPs utilization' verified verbatim, along with 'The MFU of PaLM 540B is 45.7% without self-attention or 46.2% with it. Our analytically computed hardware FLOPs utilization, which includes rematerialization FLOPs, is 57.8%.' Average training throughput of 238.3K tokens/sec at batch size 2048 verified. 6144 TPU v4 chips across two Pods with 3072 chips and 768 hosts each, 12-way model parallelism and 256-way fully sharded data parallelism verified verbatim. 540.35B parameters, 18432 hidden, 118 layers, 780B tokens, and the batch schedule 512 -> 1024 (step 50k) -> 2048 (step 115k) to step 255k all verified.
[corrected] Megatron-LM 1T GPT (Narayanan et al. 2021) [scaling-laws]: Everything the entry does state is verified: 1T parameters, 3072 A100 80GB on Selene with NVLink/NVSwitch and eight 200 Gbps HDR InfiniBand HCAs per node in a three-level fat-tree with 850 switches, A100 16-bit peak 312 teraFLOP/s, 163 teraFLOP/s per GPU, 502 petaFLOP/s aggregate, 52% of peak, 140 teraFLOP/s per GPU for GPT-3 175B on 1024 GPUs at batch 1536, the 450-billion-token assumption, sequence 2048, vocab 51,200, PTD-P with interleaved schedule and scatter/gather. TWO CORRECTIONS. (1) The entry marks hidden size, layers and batch as 'NOT READABLE' because Table 1 is an embedded image in the arXiv HTML. That is true of the HTML but the values are fully recoverable from the arXiv PDF, which I extracted: the 1T row is 1008.0B / 160 heads / hidden 25600 / 128 layers / TP 8 / PP 64 / 3072 GPUs / batch 3072 / 163 TF per GPU / 52% / 502.0 PF/s. The sibling systems-papers row quotes the whole table correctly. (2) The entry omits the metric caveat that matters most here: eq. (3) 'assumes activation recomputation and takes into account the floating-point operations associated with the extra forward pass', so '52% of theoretical peak' is recomputation-inclusive and is not comparable to an MFU figure.
[corrected] Megatron sequence-parallelism benchmarks 22B/175B/530B/1T (Korthikanti et al. 2022) [scaling-laws]: Every number verified in arXiv:2205.05198. Configurations verified (22B: 64 heads / 6144 / 48 layers / TP8 / PP1 / 8 GPUs / batch 4 / micro 4; 175B: 96 / 12288 / 96 / TP8 / PP8 / 64 / 64 / 1; 530B: 128 / 20480 / 105 / TP8 / PP35 / 280 / 280 / 1; 1T: 160 / 25600 / 128 / TP8 / PP64 / 512 / 512 / 1). MFU/HFU pairs verified exactly: 41.5%/43.7%, 51.4%/52.8%, 56.0%/57.0%, 56.3%/57.0%, with the paper's own sentence 'we reach a MFU and HFU of 56.3% and 57.0%, respectively'. The 8-way data-parallel 530B case verified (2240 GPUs, 37.83 -> 39.15 s, MFU 56.0% -> 54.2%). A100 peak 312 teraFLOPs/sec, sequence 2048, vocab 51200, no data parallelism in the main measurements, three interleaving stages all verified. CORRECTION: table numbering. The entry cites 'Table 1 (configs)' and 'Table 2' for the MFU/HFU results; the paper's own text says the four configurations are 'listed in Table 3' and 'Both the MFU and HFU are provided in Table 5'. Table 1 is the variable-name glossary and Table 2 is activation memory per transformer layer. Values right, labels wrong.
[confirmed] MegaScale 175B strong-scaling study (Jiang et al. 2024) [scaling-laws]: arXiv:2402.15627 verified, matching the systems-papers row. Model config 175B / 128 heads / 12288 hidden / 96 layers / TP 8 / PP 8; both MFU curves verified value-for-value at batch 6144 (59.1 / 57.3 / 54.9 / 55.2 with tokens/s 531.9k / 1030.9k / 1315.6k / 1984.0k) and batch 768 (65.3 / 63.5 / 61.3 / 59.0 with tokens/s 49.0k / 95.1k / 136.7k / 176.9k), plus the Megatron-LM baselines. Caption's 300B-token normalisation, sequence 2,048, vocab 64,000, the >10,000-Ampere-GPU production statement and the Broadcom Tomahawk 4 / three-layer CLOS / multi-rail network description all verified. The paper labels the metric MFU explicitly.
[confirmed] Llama 3 405B (Grattafiori/Dubey et al. 2024) [scaling-laws]: arXiv:2407.21783 verified. All three Table 4 stages verified exactly (8,192 GPUs TP8/CP1/PP16/DP64 -> 430 TFLOPs/GPU and 43% MFU; 16,384 TP8/CP1/PP16/DP128 -> 400 and 41%; 16,384 TP8/CP16/PP16/DP8 at 131,072 seq -> 380 and 38%), with 16M tokens per batch held constant across all three. The 'BF16 Model FLOPs Utilization (MFU ...) of 38-43%' sentence and its DP=128-vs-DP=64 explanation verified verbatim. Hardware verified (up to 16K H100 at 700W with 80GB HBM3, Grand Teton, NVLink intra-node, RoCE on Arista 7800 + Minipack2 at 400 Gbps, Quantum2 InfiniBand for smaller models), as is 15.6T tokens and FSDP for the DP dimension. One self-declared gap worth closing rather than an error: the entry marks hidden size as not quoted, but Table 3 gives model dimension 16,384 (with FFN 53,248, 126 layers, vocab 128,000) — the sibling western-moe row has it.
[confirmed] DeepSeek-V3 (DeepSeek-AI 2024) [scaling-laws]: arXiv:2412.19437 verified in full, matching the deepseek-qwen row: 671B/37B, 61 layers, hidden 7168, 256 routed + 1 shared, expert intermediate 2048, top-8 with at most 4 nodes, 58 MoE layers implied by 'all FFNs except the first three', 14.8T tokens at 4K, batch 3072 -> 15360 over the first 469B, 2048 H800 with NVLink/NVSwitch (160 GB/s) and InfiniBand (50 GB/s), PP16 DualPipe + EP64 across 8 nodes + ZeRO-1 DP with no tensor parallelism, 180K GPU-hours per trillion tokens, 2.664M and 2.788M totals. The critical negative is verified: a case-insensitive scan for MFU, 'model flops utilization' and 'hardware flops' returns zero hits in the tech report. The derived tokens/s arithmetic reproduces (180,000 GPU-h / 2048 = 87.89 h = 316,406 s per trillion tokens -> 3.16M tokens/s -> 1,543 tokens/s/GPU), and the explicit refusal to convert that into an MFU for lack of a stated peak-FLOPS denominator is correct.
[confirmed] Pangu Ultra MoE 718B (Huawei 2025) [scaling-laws]: arXiv:2505.04519 verified, matching the other two rows for this model: 718B/39B, hidden 7680 and 61 layers from Table 2 Model 7, 256 routed experts with 1 shared and always 8 activated, intermediate 2048 for both shared and routed, MFU 30.0% with TPS 1.46M on 6K Ascend NPUs against baseline 18.9% and 0.61M on 4K, Table 5's cumulative +15.8 / +28.6 / +49.2 / +58.7, the 'relative 10% improvement in MFU' from dynamic expert placement, and TP=8 / PP=16 / VPP=2 / EP=4 / MBS=2 stated twice. The caveats are accurate: the paper never names the silicon generation of the 6K production cluster (Ascend 910B appears only as the simulator's calibration target), no token count is given, and the sequence length 8192 belongs to the separate 128-NPU 4.2B validation run.
[confirmed] Skywork-MoE 146B (Wei et al. 2024) [scaling-laws]: arXiv:2406.06563 verified. 146B total / 22B activated, architecture table (vocab 65,536, hidden 4,608, FFN 12,288, head dim 128, 36 heads, 52 layers, 16 total experts, 2 routed, MoE layer frequency 1, native sequence 8192), the Appendix B.3 cluster paragraph verbatim (192 NVIDIA-HGX-A800 nodes = 1536 A800-80G SXM, 400 GB/s NVLink, 800 Gb/s RoCE, 12-way PP, 4-way tensor-expert via EDP, 32-way DP with ZeRO-1) and '38% Model Floating-point Utilization (MFU) on the cluster and a throughput of 690 tokens per GPU per second'. Unlike the chinese-large row for this model, this entry does not carry the misattributed shared-expert quote — it states 16 experts and top-2 and stops there, which is the correct scope. Reporting the total token count as not stated is right; the paper only describes a curated SkyPile subset plus synthetic data on top of a Skywork-13B checkpoint.
[confirmed] MegaScale-MoE Internal-352B (ByteDance 2025) [scaling-laws]: arXiv:2505.11432 verified, matching the systems-papers row: Table 2 'Internal-352B | 60 | 4096 | 32 | 4 | 14336 | 32 | 3', Table 4 H800 spec row '989 | 80 | 3.4 | 400', all ten strong-scaling throughput cells, and the MFU sentence 'declines from 32.48% to 27.89%' with its fixed-batch/pipeline-bubble explanation. Sequence 8,192, vocab 65,536, PP 15 for both systems, global batch fixed at 720, weak scaling 480->1440 GPUs with batch 360->1080 and 2.74% versus 0.2% degradation all verified. The MoE-specific hardware insight quoted at the end ('the MFU value decreases as GPU compute capability increases ... memory bandwidth does not scale as quickly as compute capabilities') is verbatim and is a genuinely useful observation for the paper.
[confirmed] Mixtral 8x22B under MoE Parallel Folding (NVIDIA) [scaling-laws]: arXiv:2504.14960 verified — and notably this row resolves a conflict with the systems-papers row in its own favour. Sec 4.3 states 'The global batch size is set to 1024 in the scaling experiments', so this entry's 'strong-scaling at fixed global batch 1024' is correct while the systems-papers row's 256 is not; the 256 belongs only to the Table 1 / Table 3 strategy comparison, which this entry also labels correctly. All MFU values verified: Folding 52.2 / 50.7 / 48.9 / 44.9, MCore 49.4 / 48.0 / 45.5 / 42.3, TP+EP+DP 40.4 / 39.1 / 36.0 / 36.2, FSDP+EP 23.9 / 25.5 / 24.4 / 23.8; and at batch 256 on 128 GPUs Folding 49.3%, MCore 46.3%, TP+EP+DP 36.6%, FSDP+EP 23.4%, FSDP 4.3%; G8T8 at 28.8% / 17.1% / 2.2%. Eos hardware and BF16 measurement basis verified.
[confirmed] Qwen2-57B-A14B under MoE Parallel Folding (NVIDIA) [scaling-laws]: arXiv:2504.14960 verified. 'For the fine-grained MoE, we choose Qwen2-57B-A14B, which has 64 experts and 8 active experts per token, totaling 57 billion parameters with 14 billion active parameters' verbatim. Full Table 4 curve verified: Folding 39.9 / 39.7 / 38.1 / 36.6 / 33.4 across 64-1024 GPUs, MCore 36.2 / 36.0 / 34.8 / 32.5 / 29.8, FSDP+EP 26.3 / 25.6 / 23.4 / 22.8 / 21.6, TP+EP+DP 22.4 / 20.9 / 19.8 / 19.7 / 17.9. Abstract's 39.0% and the long-context 38.7% -> 35.9% verified. Global batch 1024 for the scaling sweep and 4096 sequence length verified. Correctly notes the paper restates none of the widths.
[corrected] DeepSeek-V3-685B under Megatron-Core MoE (NVIDIA 2026) [scaling-laws]: Table 11 verified exactly (GB300/256 MXFP8 1233 TF and 4,730 tokens/s/GPU; GB200 MXFP8 1048 and 4,020; GB200 BF16 857 and 3,298; H100/1024 FP8-BLK 368 and 1,412), as is 685B = 671B main + 14B MTP, 37B active, force-balanced routing, and the GB300/GB200 Table 20 config 'TP1 PP4 CP1 EP64 VPP4 MBS1 GBS8192'. CORRECTION: the entry writes the H100 configuration as 'TP2/PP8 per the case study text' on the strength of the sentence 'GB200's 192 GB per GPU (vs. H100's 80 GB) allows TP1/PP4 instead of TP2/PP8'. That sentence is a general platform comparison in the Sec 9.2 case study; the actual benchmark configuration behind the 368 TF / 1,024-H100 row is given in Appendix B Table 20 as 'TP2 PP4 CP1 EP64 VPP4 MBS1 GBS8192' — TP2 with PP4, not PP8. The sibling systems-papers row has this right. Separately, the quoted 'at around 50% MFU' remark is genuine but is a stated assumption inside an optimizer-offloading trade-off discussion, not a measured result for any tabulated run — worth labelling as such if used.
[confirmed] Qwen3-235B under Megatron-Core MoE (NVIDIA 2026) [scaling-laws]: arXiv:2603.07685 Table 11 verified exactly: GB300/256/4,096/MXFP8 -> 974 TF and 6,583 tokens/s/GPU; GB200 MXFP8 -> 919 and 6,212; GB200 BF16 -> 750 and 5,100; H100/256/BF16 -> 320 and 2,132; GB300/128/131,072/MXFP8 -> 1,150 and 1,556. Table 20 configs verified, with the GB300 and GB200 rows printed as 'VPP 0 6' — reading them as VPP=6 is reasonable and the MBS 2 / MBS 3 and GBS 3072 values are exact. The 129%-of-short-context-MFU sentence verified verbatim including its parenthetical explanation. Correctly reports that no MFU is given per row and that the paper restates none of Qwen3-235B's widths or expert counts.
[confirmed] Hunyuan-Large 389B (Tencent 2024) [scaling-laws]: arXiv:2411.02265 verified. Table 1 caption and rows exactly as transcribed: 389B total / 52B activated, 1 shared plus 1 activated specialized expert per token, 64 layers, 80 attention heads, 8 key/value heads, 16 specialized experts, 7T trained tokens, SwiGLU, vocab 128K, hidden size 6,400. The 'approximately 58.1B optimal, 52B selected' scaling-law derivation is also in the paper. Negative findings verified: the report has no infrastructure section at all — no accelerator type or count, no interconnect, no MFU/HFU/TFLOPS/tokens-per-second, and no FFN or expert intermediate size anywhere.
[confirmed] Ling-Plus 290B (Ling Team / Ant Group 2025) [scaling-laws]: arXiv:2503.05139 verified. 290B total / 28.8B activated (and Ling-Lite 16.8B/2.75B), 9T tokens across five hardware configurations, the anonymised device table (A 370/64/7, B 120/96/4.5, C 312/80/10, D 989/80/27.5 with FP8, E 147/96/5.64 with FP8), and the cost sentence verbatim (~6.35M RMB per trillion tokens on device D versus ~5.08M on lower-spec, ~20% savings). This row captures two details the systems-papers row omits, and both are verified: the batch warmup from 2,560 to a maximum of 8,960 with sequence length fixed at 4K, and the 150B tokens processed in the long-context stage at 16K. Correctly reports no MFU and no tokens/s for the training run — cost per trillion tokens is the paper's only efficiency metric.
[confirmed] Clark et al. routed-language-model sweep (2022) [scaling-laws]: arXiv:2202.01169 verified. The training-scale sentence is verbatim: 'Models were trained with a sequence length of 2048 and batch size of 256 for 250,000 steps, i.e. 130 billion tokens, regardless of N', along with 'All models are trained on TPUs with JAX using a combination of data, expert and sharding parallelism' and ZeRO Stage 1 — so TPU generation and chip count are genuinely unstated. 'We default to K=1' verified, as is E swept over {2,...,512}, '168 different models, including dense baselines', and 'typically using 4 or 8 local experts per device'. All seven parameter counts (16,527,360 / 27,279,360 / 57,369,600 / 132,163,584 / 368,123,904 / 872,546,304 / 1,308,819,456) are present in the architecture table. One nuance for the author: the sweep text says the 168 models span 'six model sizes (described in Table 4)' while the table itself lists seven rows — worth a footnote if the seven-size list is reproduced. No MFU/HFU/TFLOPS/tokens-per-second anywhere, correct.
[confirmed] Ludziejewski et al. fine-grained-MoE (granularity) sweep (2024) [scaling-laws]: arXiv:2402.07871 verified. The setup sentence is verbatim: 'We run over 100 experiments ... training models with sizes ranging from 129M to 3.7B parameters across different training durations, from 16B to 130B tokens. We consider logarithmically spaced values of granularity between 1 and 16. To constrain the search space, E=64 is fixed, following the recommendations of Clark et al.' The only hardware-adjacent figure is the Figure 5(b) caption, verified verbatim: training loss curves for N = 64 x 7M and D = 66B tokens measured against wall-clock time on an NVIDIA A100, with G=8 best and G=16 dominated by routing cost. Correctly reports no MFU or TFLOPS, and no accelerator count.
[confirmed] Abnar et al. optimal-sparsity sweep (2025) [scaling-laws]: arXiv:2501.12370 verified. 'We use the Megablocks library to train dropless MoEs in which the routing mechanism ensures that all tokens are efficiently routed without being dropped due to routing capacity constraints' and 'We also fixed our batch size to 1024 for all model sizes' both verbatim. Data setup verified: RedPajamaV1 (1.2 trillion tokens), GPT-NeoX tokenizer with vocabulary 50,432, effective dataset size adjusted by compute budget C and model size N. The sparsity sweep with experts and top-k adjusted to hit the target sparsity verified. Correctly reports no hardware section, no accelerator, and no throughput metric of any kind.
[confirmed] Ludziejewski et al. Joint MoE Scaling Laws sweep (2025) [scaling-laws]: arXiv:2502.05172 verified. The fitted-coefficient table contains 35.91, 2.0732, 290.4521 and 1.3637 as transcribed, and the fit-quality sentence is verbatim: 'We observe a good fit with RMSE_v = 0.0039 on a held-out set of our 30 runs with the lowest loss, and RMSE_t = 0.0062 on the training dataset', with LBFGS following Hoffmann et al. The functional form matches. Correctly reports no accelerator, no count and no throughput for the runs; the H100 / 8xH100 / RTX4090 figures are memory constraints used in the analysis, not run hardware, and the entry says so. The quoted mechanism sentence about small batch sizes causing low hardware utilization is genuinely in the paper.
[confirmed] Kaplan et al. dense scaling-law sweep (2020) [scaling-laws]: arXiv:2001.08361 verified. The data/metric sentence is verbatim: 'We train language models on WebText2, an extended version of the WebText dataset, tokenized using byte-pair encoding with a vocabulary size n_vocab = 50257. We optimize the autoregressive log-likelihood (i.e. cross-entropy loss) averaged over a 1024-token context, which is also our principal performance metric.' The law is stated in terms of non-embedding parameters as the entry says, and C ~= 6NBS with results quoted in PF-days. Correctly reports no accelerator, no count and no throughput — the paper genuinely gives none. The entry makes no numeric claims beyond these, so there is nothing over-stated.
[confirmed] Zhang et al. critical-batch-size sweep (2024/25) [scaling-laws]: arXiv:2410.21676 verified. The setup sentence is verbatim: 'We train a series of autoregressive LMs with a context length of 512 in different sizes ranging from 85M, 151M, 302M, 604M to 1.2B on C4 using Adam', confirming all five model sizes. The abstract's headline verified: 'our results demonstrate that CBS scales primarily with data size rather than model size'. The Chinchilla token size of 3.072B for the 151M models verified. The data-parallel framing verified ('fully synchronized distributed data parallelism scenarios'). Correctly reports no hardware and no throughput metric — the paper measures optimization steps, not wall clock. Two details the entry does not claim but which are in the paper and may matter: context length is 512, and the tokenizer is gpt-neox-20b with vocabulary 50280.
[confirmed] Llama-13B / 30B / 65B parallelisation-layout sweep (Hagemann et al. 2024) [scaling-laws]: arXiv:2311.05610 verified. 'a Model FLOPs utilization of 70.5% when training a Llama 13B model' verified verbatim, as is the comparison sentence: 'we outperform MPT-70B, the original Llama-65B model by Meta, and the Megatron-LM 76B model with an MFU of 59.6% compared to 53.3%, 49.4%, and 34.7%, respectively', and 'MFU improvements of 6-18 MFU percentage points over the previous state-of-the-art'. Hardware verified (up to 32 NVIDIA DGX A100 nodes, eight A100 80GB each, 256 GPUs total, third-generation NVLink, 200 Gb/s HDR InfiniBand), as are the batch sizes (2,048 at 2k sequence length, 512 at 8k), the full Cartesian sweep over TP/PP/micro-batch/activation-checkpointing, and the central finding 'a micro-batch size of 1 achieves the highest MFU'. The 52-attention-head divisibility note for the 30B model is also verbatim.
[confirmed] DeepSeek LLM 7B/67B scaling-law study (2024) [scaling-laws]: arXiv:2401.02954 verified. The HAI-LLM sentence is verbatim: 'Data parallelism, tensor parallelism, sequence parallelism, and 1F1B pipeline parallelism are integrated into this framework as done in Megatron', with ZeRO-1, flash attention and bf16 with fp32 gradient accumulation — and no parallelism degrees given. The negative findings are verified: zero hits for A100, H800 or TFLOPS, so the infrastructure section genuinely names no GPU type or count, and there is no MFU or tokens/s figure. 2.0T tokens for the flagship models verified, as is the 67B row (95 layers, d_model 8192) and the (8/3)d_model FFN rule.
[corrected] Switch-XXL: THE ONLY RUN FAILING THE 15% ARITHMETIC TEST (-49%). Extraction is faithful — I pulled arXiv:2101.03961 LaTeX source and Table 9 reads exactly as transcribed (395B, GEGLU, d_model 4096, d_ff 10240, Expert Freq 1/2, Num. Layers 24, Num Experts 64). The defect is in the paper. Counting convention validated on the three sibling rows (layers are per-stack, encoder+decoder both counted): Switch-Base 7B vs 7.4B calc (+5.9%), Switch-Large 26B vs 27.1B (+4.3%), Switch-C 1571B vs 1571.2B (+0.0%). Applying the same convention to Switch-XXL (24 layers -> 48 FFN blocks, freq 1/2 -> 24 MoE blocks, 64 experts x 3x4096x10240) yields 201.3B, not 395B. Single-cell sensitivity: if Num Experts were 128 (as for Switch-Base and Switch-Large) -> 394.5B, essentially exact; if Expert Freq were 1 -> 391.5B; if Num. Layers were 48 -> 397.6B. The paper's text uses 395B consistently ('395B vs 1.6T', '4x less unique parameters'; 1571/395 = 3.98), so 395B is the load-bearing figure and one of the architecture cells is wrong — most likely the expert count. ACTION: print 395B total, but do NOT print '64 experts' as reliable; flag the row as internally inconsistent.
[corrected] MegaScale — 175B GPT-style transformer (DENSE): Paper-internal defect in Table 2, transcribed faithfully. I pulled arXiv:2402.15627 source (sections/experience.tex:64): the 12,288-GPU MegaScale row does print '55.2% ... 2166.3'. But that aggregate PFLOP/s is inconsistent with its own row. Every other MegaScale row implies ~1.065e12 model FLOPs/token (256:1.0653, 512:1.0662, 768:1.0746, 1024:1.0656, 3072:1.0650, 6144:1.0655, 8192:1.0646); the 12,288 row implies 1.0919e12. Cross-checks: 2166.3e15/(12288 x 312e12) = 56.5%, not the printed 55.2%; the value consistent with BOTH the printed MFU and the printed 1984.0k tokens/s is 2113-2116 (55.2% -> 2116.3; 1.065e12 x 1984.0k -> 2113.0). Looks like a digit transposition of 2116.3. Speedup cross-check also splits: iteration time gives 1.3517x, tokens/s 1.3526x, MFU 1.3398x (paper prints 1.34x), PFLOP/s 1.3715x. ACTION: quote the MFU (55.2%) and tokens/s (1984.0k) — both self-consistent — and do not quote 2166.3. The Megatron-LM baseline aggregate-PFLOP/s cells at batch 768 are similarly unreliable (256 GPUs: printed 43.3 vs 42.3 implied; 1024 GPUs: printed 131.9 vs 142.9 implied), while all its MFU and tokens/s cells are consistent.
[corrected] MoE Parallel Folding / Megatron-Core — Mixtral 8x22B: The discrepancy the extraction flagged as unresolved (49.3%/46.3% in Tables 1&3 vs 52.2%/49.4% in Table 4, both at 128 GPUs) IS resolved, and it is not a contradiction: the two experiments use different global batch sizes. Verified verbatim in arXiv:2504.14960 source — neurips_2024.tex:674 'the global batch size was fixed at 256' (optimal-config experiment, Tables 1/3) and neurips_2024.tex:472 'The global batch size is set to 1024 in the scaling experiments' (Table 4). The sibling scaling-laws extraction of this same paper already has this right. Independent corroboration: Table 2's per-GPU TFLOPS divided by the stated H100 BF16 peak reproduces Tables 1/3 exactly — 487.7/989.5 = 49.3% (Folding) and 458.3/989.5 = 46.3% (MCore). ACTION: label every MFU from this paper with its batch size; the batch-1024 curve (52.2 -> 50.7 -> 48.9 -> 44.9% over 128->1024 GPUs) is the one to plot as the MoE MFU-vs-scale curve.
[corrected] MoE Parallel Folding / Megatron-Core — Llama3-8x70B: The suspected typo is confirmed as printed and the extraction's reasoning is validated. In arXiv:2504.14960 source (tables/detailed_GPUs_scaling.tex) the Llama3-8x70B block really does label MCore w/ Folding and FSDP+EP rows '128' while the MCore rows start at 256, and Table 3 (tables/detailed_parallelism.tex) configures this model only at 256 GPUs. Corroborating evidence the table is unpolished: its first source line is the literal comment '% TODO: clean this table'. Treat the '128' entries (43.7% Folding, 18.9% FSDP+EP) as 256-GPU points; the body text describes exactly this series as dropping 'from 43.7% to 41.5%'. Transcribing as printed rather than silently correcting was the right call.
[corrected] Qwen3-30B-A3B (pre-training run): The UNVERIFIED flag can be lifted. I fetched huggingface.co/Qwen/Qwen3-30B-A3B/raw/main/config.json (HTTP 200) and it confirms hidden_size 2048 and moe_intermediate_size 768, plus num_experts 128, num_experts_per_tok 8, num_hidden_layers 48, num_attention_heads 32, num_key_value_heads 4, head_dim 128, intermediate_size 6144, decoder_sparse_step 1, mlp_only_layers [], vocab_size 151936, tie_word_embeddings false. Arithmetic corroborates independently: those values reconstruct 30.5B total (+1.7% vs stated 30B) and 3.0B active (+1.0% vs 3B). Provenance is still a first-party release artifact, not the technical report — keep that caveat, but drop 're-check before use'.
[corrected] Hunyuan-Large: The 'moe_intermediate NOT STATED' gap can be closed, and I confirmed the value arithmetically before recommending it. MegaScale-MoE (arXiv:2505.11432) Table 2 lists Hunyuan-Large with h_ffn 18304, and that is the only value that reconciles Tencent's own stated 389B: with 6400 hidden, 17 experts (16 specialized + 1 shared), 64 layers and SwiGLU, intermediate 18304 -> 389.5B total (+0.1%) and 51.3B active (-1.4% vs stated 52B). Sensitivity: 16384 -> 349.7B (-10%), 20480 -> 435.2B (+12%). So 18304 is pinned to within about +/-3%. Cite it as ByteDance's restatement in a training-systems paper, not as Tencent's own figure. Everything else in the row is confirmed verbatim; note this run duplicates the scaling-laws 'Hunyuan-Large 389B' row, which has the same gap.
[corrected] Switch-C: Total is exact (1571.2B calc vs 1571B stated, +0.0%) but the layer description understates the model. The 1571B only reconciles if 'Num. Layers 15' is read as 15 per stack with BOTH encoder and decoder counted, i.e. 30 MoE FFN blocks (2048 experts x 2x2080x6144 x 30 = 1570.3B); reading it as 15 MoE layers halves the model. Same convention is what makes Switch-Base and Switch-Large reconcile. Recommend phrasing as 'expert frequency 1 over 15 encoder + 15 decoder layers = 30 MoE blocks'. Minor separate fix: the run reports '64 heads, d_kv 32', but Table 9's header order is d_kv then Num. Heads, so Switch-C is d_kv 64, Num. Heads 32 (the product is unchanged, so no downstream arithmetic is affected). Neither is a required field.
[corrected] Megatron-LM 1T GPT (Narayanan et al. 2021) [scaling-laws]: The 'hidden NOT READABLE — Table 1 is an embedded image' limitation is real for the arXiv HTML but is already solved elsewhere in this same corpus: the systems-papers stream extracted Table 1 from the PDF and has the full row (1T model: hidden 25600, 128 layers, 160 heads, TP 8, PP 64, 3072 GPUs, batch 3072, 163 TF/GPU, 52% of peak). Cross-fill rather than reporting unknown. I verified the whole table arithmetically: every printed 'percentage of theoretical peak' equals TFLOPs/GPU divided by the stated 312 TF A100 peak (137->43.9%, 142->45.5%, 163->52.2%), and 12 x hidden^2 x layers reproduces all ten parameter counts within 1.2%.
[corrected] Llama 3 405B (Grattafiori/Dubey et al. 2024) [scaling-laws]: This copy reports hidden as 'NOT quoted / treat as unknown', but the western-moe 'Llama 3.1 405B (DENSE contrast)' row has the full Table 3 verbatim (Model Dimension 16,384, FFN Dimension 53,248, 126 layers, 128 heads, 8 KV heads, vocab 128,000). Cross-fill. Those values recompute to 405.8B vs 405B stated (+0.2%), so they are sound. The two rows are otherwise the same run and should be merged — this is the strongest dense contrast in the set (both TFLOPs/GPU and explicitly-labelled BF16 MFU at three configurations, with full 4D parallelism degrees).
[corrected] Ling-Plus 290B (Ling Team / Ant Group 2025) [scaling-laws]: Merge with the systems-papers 'Ling-Plus / Ling-Lite' row: this copy is materially richer on the tokens/batch axis (9T stage-1 plus 150B long-context, max seq 4K extended to 16K, batch warmed 2,560 -> 8,960), which the systems-papers copy lacks. arXiv:2503.05139 verified to exist; exact title is 'Every FLOP Counts: Scaling a 300B Mixture-of-Experts LING LLM without Premium GPUs'. Both copies correctly flag the 290B-abstract vs 300B-title mismatch. Accelerator identity is unrecoverable by design (devices A-E given only by peak FLOPS/memory/cost), and there is no MFU or tokens/s anywhere — the efficiency metric is RMB per trillion tokens.
[unsourced] DBRX (dbrx-base): Negative finding independently reproduced: I fetched huggingface.co/databricks/dbrx-base/raw/main/config.json AND .../dbrx-instruct/raw/main/config.json — both return HTTP 401. So hidden, moe_intermediate and n_moe_layers genuinely have no reachable primary source, and the caution against the circulating d_model 6144 / ffn_hidden_size 10752 / 40-layer figures stands. Worth recording for the author's judgement: those circulating dims ARE arithmetically consistent with Databricks' own published 132B/36B (they reconstruct 131.6B total, -0.3%, and 35.8B active, -0.4%), so they are very likely correct — but consistency is corroboration, not sourcing. Everything Databricks does publish (132B, 36B, 16 experts, top-4, 12T tokens, 32k context, 3072 H100, 3.2 Tbps InfiniBand) is properly first-party sourced. The refusal to derive a training throughput from the 'three months' figure is correct — that window explicitly covers post-training, evaluation and red-teaming.
[unsourced] Llama 4 Scout (17Bx16E): Meta publishes no dims and the HF config is license-gated, so hidden, moe_intermediate, top_k, n_moe_layers and accelerator_count are all correctly reported unknown. Corroboration only, not for citation: the circulating config (hidden 5120, moe intermediate 8192, 48 layers all-MoE, 16 experts, top-1 plus one shared) reconstructs 107.8B vs stated 109B (-1.1%) and 16.1B vs 17B active (-5.1%), which also independently supports top-1 routing and the shared expert that Meta's released llama4/moe.py constructs unconditionally. Derived throughput arithmetic is exact (40e12 / (5.0e6 x 3600) = 2,222.2 tokens/s/GPU), and the stated caveat is the right one — the card defines its GPU-hours as total training time, not pretraining-only. Keeping the blog's 32K GPUs and 390 TFLOPs/GPU out of this row (they are Behemoth-only) is correct.
[unsourced] Llama 4 Maverick (17Bx128E): Same gating as Scout: hidden, moe_intermediate, n_moe_layers, accelerator_count all genuinely unavailable. Corroboration only: the circulating config with alternating dense/MoE (48 layers -> 24 MoE with 128 routed + 1 shared at moe intermediate 8192, 24 dense at 16384, hidden 5120) reconstructs 400.7B vs stated 400B (+0.2%) — an unusually tight fit that supports Meta's prose description of the layer alternation. Derived rate is exact (22e12 / (2.38e6 x 3600) = 2,567.7 tokens/s/GPU). The extra caveat is important and correctly stated: Maverick was codistilled from Behemoth during pretraining, so teacher forward passes may sit inside the 2.38M GPU-hour denominator.
[unsourced] Llama 4 Behemoth (preview, still training): No dims, no top-k, no layer count, no token count specific to Behemoth, and the accelerator type behind '32K GPUs' is never named. The 390 TFLOPs/GPU is the only accelerator-level throughput figure Meta published for Llama 4 and it is FP8 and Behemoth-only. The refusal to divide it by an H100 peak to back out an MFU is exactly right — with no stated GPU type there is no defensible denominator, and Meta pointedly claims 'high model FLOPs utilization' without printing a percentage. Usable only as a single FP8 TFLOPS/accelerator datapoint with a stated-unknown hardware caveat.
[unsourced] Yuan 2.0-M32: The run cannot satisfy the spec and I could not run the requested recompute from its own fields: moe_intermediate is NOT STATED and the layer count is NOT STATED, so total parameters cannot be checked against experts x expert size x layers. Accelerator, count, throughput and parallelism degrees are all absent too. A plausible reconstruction (24 layers inherited from the Yuan 2.0-2B base, intermediate 8192) lands at 39.6B vs stated 40B, but two of its four inputs are assumptions, so I am not reporting it as a check. Recommend keeping only the sourced headline (40B total, 3.7B active, 32 experts, top-2, hidden 2048, 2000B tokens, seq 4096) and marking the rest unknown.
[unsourced] Comet — Mixtral-8x7B, Qwen2-MoE, Phi3.5-MoE (ByteDance): Almost every required architecture field (hidden, moe_intermediate, n_experts, top_k, n_moe_layers) is absent, and the throughput is speedup ratios only — no MFU, TFLOPS or tokens/s. The one sourced pair is Mixtral-8x7B's 45B total / 14B active, which conflicts slightly with Mistral's own 47B/13B (Comet is restating a third-party model loosely). Cluster size is given only for the 8-H800 breakdown figure. Usable as a communication-overlap citation, not as a training-run row.
[drop] Kimi k1.5: Contributes nothing to any required field and the report withholds architecture deliberately — it does not even state whether k1.5 is dense or MoE, which is why the run has no dense_or_moe value at all. The appendix explicitly defers: specifics 'lie beyond the scope of this report'. No accelerator, count, throughput, token total, or parallelism degree. The warning not to carry K2's numbers across is the right instinct. Drop from the table, or keep one line in the text as an example of a withheld-architecture release.
[drop] Step-2 (StepFun): No primary source exists and the negative evidence is unusually strong and correctly gathered: StepFun's own Step-3 paper (arXiv:2507.19427) cites Step-2 exactly once, and the bibliography entry resolves to an API documentation page, not a report. Every field is unknown. Drop the row; do not substitute Step-3's numbers, as the run correctly warns.
[drop] FasterMoE (PPoPP '22): Primary source inaccessible (ACM DL 403, not on arXiv, no author-hosted mirror found) and the run holds zero verbatim quotes — every field is UNKNOWN. The search-engine paraphrase of the abstract (16 V100s, 1.37x-17.87x speedups) is explicitly unverified and must not be cited. Drop unless the PDF is obtained through institutional access; if it is, the row can be rebuilt.
[drop] Janus (SIGCOMM '23): Same situation: ACM DL 403, not on arXiv, no verbatim quote from the paper itself, all fields UNKNOWN. The only verifiable statement is second-hand — MegaScale-MoE's related-work characterisation of Janus's data-centric design. Drop as a data row; it can survive as a design-space citation described through MegaScale-MoE's sourced sentence.
[drop] ScheMoE (EuroSys '24): ACM DL 403 and OpenReview 403; no verbatim quote, all fields UNKNOWN. The run identifies the right substitute: the same group's FSMoE (arXiv:2501.10714) is open and covers the same design lineage, and HierMoE (arXiv:2508.09591, which I verified exists) describes ScheMoE's contribution as 'PipeA2A'. Drop the data row and cite ScheMoE only through those.
[drop] MixNet — optical-electrical reconfigurable fabric: Contributes no model architecture, no token count, and no throughput in any of the four requested units — its metric is networking cost efficiency (performance per dollar), and the four 'representative MoE models' are never named or sized. The 32 A100s are a prototype, with results otherwise simulated to 30K+ GPUs. Drop from a training-run/throughput table; keep only if the paper has a section on interconnect fabrics, where it is a legitimate citation.
[confirmed] DeepSeek-V2 (pre-training run): Architecture reconciles tightly: 160 routed + 2 shared experts at 3 x 5120 x 1536 over 59 MoE layers, plus MLA and the one dense FFN, gives 235.7B vs stated 236B (-0.1%) and 20.8B vs stated 21B active (-0.9%). Derived rate is exact: 1e12/172,800/3600 = 1,607.5 tokens/GPU/s. All three honest flags are correct — the GPU count really is never stated (so no cluster tokens/s is derivable), '59 of 60' really is derived rather than printed, and the >50K tokens/s figure really is inference serving, not training. Reporting the cost rate as a cost rate rather than converting it to MFU is the right call.
[confirmed] DeepSeek-V3 (pre-training run): The best-verified row in the entire set. Architecture: 256 routed + 1 shared at 3 x 7168 x 2048 over 58 MoE layers, 3 dense layers, MLA over 61 layers -> 670.9B vs stated 671B (-0.0%) and 36.5B vs 37B active (-1.3%). Every derived figure checks exactly: 1e12/180,000/3600 = 1,543.2 tokens/GPU/s; x2048 x86400 = 273.1B tokens/day against the hardware paper's measured 272.80B (0.1% apart, two independent papers); 272.80e9/86400 x 19.926 s = 62,914,500 tokens/step against 15,360 x 4096 = 62,914,560 exactly; 432/0.4373 = 987.9 implied peak TFLOPS, confirming the TFLOPS row is per-GPU BF16. Correctly separates the two papers' metrics. arXiv:2505.09343 verified to exist with the stated title.
[confirmed] DeepSeek-R1-Zero (RL run on DeepSeek-V3-Base): I pulled arXiv:2501.12948 source and verified the distinctive claims verbatim in appendix.tex: 'we employed 64*8 H800 GPUs, and the process required approximately 198 hours' (line 287) and the cost table row 'in H800 GPU Hours & 101K & 5K & 41K & 147K' (line 301). The 512 x 198 = 101,376 consistency check is exact. The flagged internal inconsistency is real and sits at line 286 — the appendix says 'scale up to 660B R1-Zero and R1' while the results table says 671B total. Flagging it rather than silently picking one is right. The 'no throughput metric of any kind' finding and the refusal to derive tokens/s (total tokens are genuinely not reported) both hold.
[confirmed] DeepSeek-R1 (cold-start SFT + two-stage RL): Verified verbatim from the same source: 'the same 64*8 H800 GPUs, completing the process in about 4 days, or roughly 80 hours. To create the SFT datasets, we use 5K GPU hours.' The 512 x 80 = 40,960 check matches the tabulated 41K. Architecture provenance handling is exactly right — the R1 paper does not restate hidden, expert width, expert count or top-k, and the row correctly inherits them from arXiv:2412.19437 with the inheritance labelled rather than presented as R1's own.
[confirmed] DeepSeek LLM 67B (DENSE contrast): Reconciles at 67.4B vs stated 67B (+0.6%) using 95 layers, hidden 8192 and the released 22016 FFN intermediate — note the paper itself only gives the rule (8/3)xd_model, so the integer width came from outside the report, which the run correctly declines to print. Derived rate exact: 1e12/300,600/3600 = 924.1 tokens/GPU/s. The two-source split is handled correctly (architecture and tokens from arXiv:2401.02954, the 300.6K GPU-hour figure from the V2 paper's comparison), as is the note that the DeepSeek LLM paper names no accelerator.
[confirmed] Qwen2-57B-A14B (upcycled from Qwen2-7B): Reconciles at 57.4B vs 57B (+0.7%) and 13.7B vs 14B active (-2.2%), with the 8 shared experts correctly modelled as one fused 20480-wide FFN (= 8 x 2560). I verified the negative finding directly against arXiv:2407.10671 source: Table 1 in content/model_tokenizer.tex gives Hidden Size 3,584 and Intermediate Size 2,560 as quoted, and a full-text scan for GPU/H100/A100/NPU/MFU/TFLOPS/cluster/Megatron returns nothing — the report genuinely carries no hardware, throughput or parallelism. The 'n_moe_layers NOT STATED' caution is also correct; only the release config implies all 28.
[confirmed] Qwen3-235B-A22B (pre-training run): I verified the negative findings against arXiv:2505.09388 source: content/arch.tex Table 2 has exactly the columns Models | Layers | Heads (Q/KV) | # Experts (Total/Activated) | Context Length — no hidden size and no intermediate size — and a full-text scan finds no accelerator, cluster, MFU, TFLOPS or parallelism anywhere; the only GPU-hours mention is the relative '1/10' distillation ablation on an 8B checkpoint, correctly excluded. Arithmetic independently corroborates the config.json values the run flagged as non-report provenance: hidden 4096 with moe_intermediate 1536 over 94 layers reconstructs 235.0B (+0.0%) and 21.5B active (-2.2%), so those two numbers are safe to use with the provenance caveat attached.
[confirmed] Qwen3-Coder-480B-A35B-Instruct: Arithmetic is excellent: hidden 6144, moe_intermediate 2560, 160 experts, top-8 over 62 layers gives 480.1B (+0.0%) and 34.5B active (-1.5%), so the config.json values are internally sound. The provenance warning is the important part and is correctly prominent — there is no paper, so every figure rests on Qwen's model card, config.json and release blog, and the 7.5T-token / 70%-code figure exists only in the blog. Correctly notes the 20,000-parallel-environments figure is RL environment concurrency, not model parallelism.
[confirmed] Qwen3-Coder-Next (80B-A3B): arXiv:2603.00729 verified to exist with the exact title 'Qwen3-Coder-Next Technical Report'. The row is honest about being nearly empty — hidden, moe_intermediate, n_experts, top_k, n_moe_layers, accelerator, count, throughput and parallelism are all genuinely absent, and the base architecture's only pointer is a vendor blog, so no primary expert counts exist. No arithmetic check is possible. Usable only for the 80B-total / 3B-active headline, the ~600B repository-level token figure and the 32,768 -> 262,144 context extension; mark confidence weak as the run does.
[confirmed] Mixtral 8x7B: Reconciles at 46.7B vs 47B (-0.6%) and 12.7B vs 13B active (-1.9%) from Table 1's own dims (4096/14336/8/top-2/32 layers), and the released config.json matches the table on every field. The negative findings are correct and the grep for GPU/H100/A100/MFU/throughput/tokens-trained/batch/cluster returning zero hits is the right way to establish them — the paper really does state no training hardware, no token count and no throughput. Correctly notes the Expert Parallelism mention is generic technique description, not this run's configuration.
[confirmed] Mixtral 8x22B: Reconciles at 140.6B vs 141B (-0.3%) and 39.0B vs 39B active (-0.1%) — the tightest active-parameter fit in the western set. Correctly identifies that no paper exists and rests only on Mistral's announcement plus its own released config.json. Independently cross-validated: MegaScale-MoE's Table 2 lists the same model as 56 layers / h 6144 / h_ffn 16384 / 8 experts / top-2, matching the config exactly. Tokens, hardware, throughput and parallelism are genuinely unpublished.
[confirmed] Grok-1: The derived expert width is correct and I reproduced it: run.py sets emb_size 48x128 = 6144 and widening_factor 8, and model.py's ffn_size gives int(8x6144)x2//3 = 32768, already a multiple of 8. Those dims reconstruct 316.5B vs xAI's stated 314B (+0.8%). The handling of active parameters is exactly right — xAI states '25% of the weights active' and never prints a count, so leaving it unstated is correct; for the record my reconstruction puts activated weights near 26% of total, consistent with their sentence but not a substitute for it. Correctly notes local_mesh_config=(1,8) is an inference mesh, not the training configuration.
[confirmed] OLMoE-1B-7B: Reconciles at 6.9B vs 6.9B (+0.2%) and 1.2B vs 1.3B active (-9.5%, within tolerance). The sanity cross-check is exact: 5.133e12 / (256 x 10 x 86400) = 23,207 tokens/s/GPU against the ablation's quoted 23,600. The most valuable discipline in this row is the metric provenance — correctly flags that the 23,600 figure comes from the 128-GPU controlled ablation, not the 256-GPU final run, and that the 10-day figure is 'approximately', so the cross-check is a sanity check rather than a reportable number. Fully specified on tokens, sequence length, batch, hardware and interconnect.
[confirmed] Phi-3.5-MoE (16x3.8B): Reconciles at 41.9B vs 42B (-0.3%) and 6.5B vs 6.6B active (-1.4%). Derived rate is exact: 4.9e12 / (512 x 23 x 86400) = 4,816 tokens/s/GPU, with the correct caveat that the card does not say the 23 days is pure pretraining wall-clock. Good discipline on the parallelism field — the pipeline+tensor+data-without-expert-parallelism recipe is a GRIN statement and is correctly hedged rather than attributed to this run. Also correct that the Phi-3 report itself carries no hardware; the cluster comes only from the model card.
[confirmed] GRIN MoE (16x3.8B): Same architecture as Phi-3.5-MoE and reconciles identically (41.9B vs 42B, 6.5B vs 6.6B active). I verified the unit caveat directly against arXiv:2409.12136 source: inputs/4-training.tex:128 the column header is literally 'Throughput Per GPU' with no unit anywhere in the table or caption ('Training Throughput of Dense and MoE on 64 H100 gpus'), and the values 34222/27962/8176/7077 are as transcribed. Refusing to supply a unit and refusing to compute an MFU (the paper gives no FLOPs-per-token term) are both correct. Also correct that the 64 H100s are the throughput study only — the accelerator for the actual 4T-token pretraining is never stated, and 512 must not be read across from the Phi-3.5-MoE card.
[confirmed] GLaM 64B/64E: Reconciles within tolerance: ungated FFN (2 matrices) at 8192 x 32768, 64 experts over 32 MoE layers gives 1,155B vs stated 1.2T (-3.7%) and 88.0B vs 96.6B active (-8.9%). Derived rate is exact: 280e9 / (574 x 3600) / 1024 = 132.3 tokens/s/chip. The most important judgement here is correct and worth preserving: the paper's 180 GFLOPs/token is an inference figure with no stated forward/backward multiplier, so it cannot be turned into an MFU — the refusal to convert is right. Correctly notes the 32 MoE layers is the extractor's arithmetic from 'every other layer', not printed.
[confirmed] GShard MoE(2048E, 36L) — 600B: Reconciles at 618.8B vs stated 600B (+3.1%) on the run's own reading of 18 MoE blocks (2048 experts x 2 x 1024 x 8192 x 18); the alternative reading of 36 MoE blocks would give 1,237B, so the run's derivation is corroborated by a factor-of-two margin. Derived throughput is exact: 0.72 steps/s x 4M tokens / 2048 cores = 1,406 tokens/s/core. Correctly notes the 18 is the extractor's arithmetic, not printed. The >85% / >30% peak-FLOPS statements are properly labelled as per-operator, not end-to-end MFU — that distinction matters and is easy to get wrong.
[confirmed] Llama 3.1 405B (DENSE contrast): Reconciles at 405.8B vs 405B (+0.2%) from Table 3's own dims. This is the single most complete row in the corpus for the paper's purpose: both TFLOPs/GPU and explicitly-labelled BF16 MFU at three configurations (430 TF/43%, 400 TF/41%, 380 TF/38%), full 4D parallelism degrees per stage, exact accelerator model and TDP, and both interconnect fabrics distinguished (RoCE for the 405B run, InfiniBand for the smaller models). The quoted causal explanation for the 43%->41% drop (lower per-DP-group batch at constant global tokens) is exactly the mechanism the paper's scale argument needs. Merge with the scaling-laws duplicate.
[confirmed] MiniMax-Text-01 (MiniMax-01): Reconciles at 457.0B vs 456B (+0.2%) and 48.0B vs 45.9B active (+4.7%) modelling the 7:1 lightning/softmax attention stack with output gates. The critical caveat is correct and must survive into the paper: the only MFU in this paper (>75%) is end-to-end INFERENCE on H20, not training, and the TGS numbers are for 1B/3B benchmark models rather than the 456B run — so this row contributes no training throughput. Also correctly captures the unusual dynamic cluster (1500-2500 H800s varying during the run), which makes any per-GPU derivation impossible, and the four-stage token schedule with no single stated total.
[confirmed] Kimi K2: Reconciles at 1,026B vs stated 1.04T (-1.3%) and 31.5B vs 32.6B active (-3.3%) using 384 routed + 1 shared at 3 x 7168 x 2048 over 60 MoE layers with MLA at 64 heads. The '60 MoE layers' derivation from 61 layers minus 1 dense is correct and correctly flagged as not printed. Good discipline on accelerator_count: the 256-GPU figure really is a model-parallel group size, not a cluster size, and the 'any multiple of 32 nodes' phrasing means the run size is genuinely unstated. Interconnect is unusually well specified (NVLink/NVSwitch intra-node, 8x400 Gbps RoCE inter-node, 2 TB RAM/node).
[confirmed] GLM-4.5: The cleanest architecture arithmetic in the entire corpus. Honouring the paper's own stated convention (include MTP layer, exclude word embeddings and the output layer) reproduces 355.1B against stated 355B (+0.0%) and 32.0B against stated 32B (+0.0%) — both essentially exact. Table 1 is transcribed field-for-field. The gap is real and correctly reported: no accelerator, no count, no interconnect, no MFU/TFLOPS/tokens-per-second and no parallelism degrees anywhere in the report; only the RL framework is described. Excellent on the tokens axis (23T, seq 4,096 -> 32,768 -> 131,072, batch warmed 16M -> 64M over the first 500B tokens).
[confirmed] GLM-4.5-Air: Reconciles at 108.0B vs stated 106B (+1.9%) and 12.3B vs 12B active (+2.2%) under the same include-MTP/exclude-embeddings convention. Table 1 transcribed field-for-field. Correctly notes that the 23T-token, 4,096-sequence, 16M->64M-batch recipe is stated for the GLM-4.5 series without Air being split out, so the token figure should not be asserted as Air-specific. Same complete absence of hardware, throughput and parallelism as the flagship.
[confirmed] Step-3 (StepFun): Internally consistent where checkable: the stated 316B total / 38B active against 61 layers with MoE on all but the first four and the last (56 MoE layers) implies an expert-parameter sparsity near 0.083, matching the paper's own 'sparsity of around 0.08 (including shared expert)'. Correctly reports n_experts, top_k and moe_intermediate as NOT STATED — they genuinely are, and the sparsity figure alone cannot pin them. The framing caveat is the key one: this is an inference/decoding co-design paper, the 4,039 tokens/s/GPU is DECODING throughput, and it states no training accelerator, no training parallelism and no token count. Include only as a StepFun datapoint, never as a stand-in for Step-2.
[confirmed] Skywork-MoE: Reconciles at 146.4B vs 146B (+0.2%) and 22.4B vs 22B active (+1.8%). The parallelism cross-check is exact and worth keeping: 12-way PP x 4-way tensor-expert x 32-way DP = 1,536, matching the stated 1536 A800s — the paper does not do this multiplication itself, so it is independent corroboration that the configuration is complete. One of only a handful of rows carrying BOTH an explicitly-labelled MFU (38%) and tokens/GPU/s (690), plus full interconnect (400 GB/s NVLink, 800 Gb/s RoCE). Correctly reports the total token count as unstated (upcycled from an in-house Skywork-13B checkpoint).
[confirmed] TeleChat2 / TeleChat2.5 / T1 — 115B (DENSE, Ascend): Reconciles at 113.3B vs stated 115B (-1.5%) from Table 1's dims. High-value row: explicitly-labelled MFU (33.8%-36.3%) with full DP/TP/PP/CP/VPP degrees at two cluster sizes on Ascend, plus the most completely specified non-NVIDIA interconnect in the set (8 HCCS NPUs/node, 200GE RoCE, two-layer non-converged Clos, NSLB). The flagged Table 2 vs Table 3 discrepancy is real and correctly characterised rather than averaged away — 6M batch tokens is the actual pre-training recipe, 1M/4M are the MFU scaling-study configurations, and they should never be merged.
[confirmed] TeleChat3-MoE 1119B: Reconciles at 1,115.9B vs stated 1119B (-0.3%) from Table 1's own fields (61 layers, hidden 5,120, 384 routed + 1 shared, expert intermediate 3072). Correctly refuses to compute active parameters — Table 1 has no activated-parameter column and no activation ratio, so it must stay unknown. The throughput handling is right: no absolute MFU/HFU/TFLOPS/tokens-per-second exists anywhere, only relative optimisation gains (15% hierarchical EP, 25-30% firmware, 10-15% CPU/kernel isolation, 3-5% IOMMU), and these must not be combined into an absolute figure. arXiv:2512.24157 verified to exist with the exact title 'Training Report of TeleChat3-MoE'.
[confirmed] TeleChat3-MoE 438B: Reconciles at 443.6B vs stated 438B (+1.3%). The framing of the 4,096-device figure is precisely right and should be preserved verbatim in the paper: it comes from the automatic-parallelisation search example, not from a stated production cluster size, so it must not be presented as 'the 438B was trained on 4,096 devices'. Active parameters correctly unknown. The global batch of 16,384 is correctly flagged as unit-ambiguous (the report pairs it with a micro-batch-number dimension).
[confirmed] TeleChat3-MoE 105B: Reconciles at 104.3B vs stated 105B (-0.7%) from Table 1 (45 layers, hidden 2,560, 192 routed + 1 shared, expert intermediate 1536, top-4). Note this is the one TeleChat3 variant with top-4 rather than top-8. Accelerator count, tokens and parallelism are genuinely unstated — only the qualitative 'trained efficiently on modest clusters' — and are correctly left unknown.
[confirmed] Pangu Pro MoE (72B-A16B, MoGE): Reconciles essentially exactly: 72.0B vs stated 71.99B (-0.0%) and 15.7B vs 16.50B active (-4.9%), with the 4 shared experts correctly included. Two judgements are important and correct. First, the training throughput is a RELATIVE MFU only ('35% relative increase', with the baseline printed as '-'), so no absolute training MFU exists — this is easy to misreport as an absolute 35%. Second, the DP degree is correctly left unstated, and the observation that 4096/(TP8 x PP5) is not an integer is a sound reason not to derive it. All per-card tokens/s figures are correctly labelled inference.
[confirmed] Pangu Ultra MoE (718B) [chinese-large]: Reconciles at 719.1B vs stated 718B (+0.2%) and 39.2B vs stated 39B active (+0.6%) with 3 dense + 58 MoE layers at hidden 7680. The reported MFU/TPS pair is internally consistent, which is a meaningful check: 6K NPUs at 30.0% MFU with 1.46M TPS and 4K NPUs at 18.9% with 0.61M TPS both imply the same ~316 GFLOPs/token against the Ascend 800T A2's 256 TFLOPS FP16 peak. The silicon caveat is correct and important — the paper names 'Ascend 910B' only as the simulator's target, never as the 6K-NPU production cluster. Also correct that the 8192-sequence/64-batch figures belong to a separate 4.2B 128-NPU validation run and must not be attributed to the 718B. Duplicates the systems-papers and scaling-laws rows.
[confirmed] Pangu Ultra 135B (DENSE, Ascend): Reconciles at 133.9B vs stated 135B (-0.8%). The parallelism cross-check is exact: 128-way DP x 8-way TP x 8-way PP = 8,192, matching the stated NPU count. Valuable as the dense Ascend counterpart to Pangu Ultra MoE on the same vendor's hardware, with the most completely specified Ascend interconnect available (HCCS full-mesh intra-node, 64 GB/device, 200 Gbps RoCE inter-node) and a clean MFU progression (43% baseline -> over 52%). The caution that the batch-size schedule figures read as sequences rather than tokens and should not be converted is correct.
[confirmed] MegaScale-MoE — Internal-352B: Reconciles at 341.3B vs stated 352B (-3.0%) from Table 2's own fields. Throughput cross-checks are exact: 1e12/(272.9e3 x 86400) = 42.4 days and 1e12/(1407.7e3 x 86400) = 8.22 days, matching the printed training-time column at both ends, and 1407.7/746.6 = 1.88x matches the printed speedup. The MFU handling is exactly right — only the two endpoints (32.48% at 240 GPUs, 27.89% at 1440) exist as text, the 480/720/960 points are figure-only, and reporting them as unknown rather than reading them off a plot is the correct discipline. Correctly declines to derive active parameters. This is the best MoE MFU-vs-scale curve on a real production model.
[confirmed] MegaScale-MoE — open-source model configurations (Table 2): I independently validated every row of this table against each model's own primary config, and all five check out: Mixtral-8x7B (4096/14336/8/top-2/32L) and Mixtral-8x22B (6144/16384/8/top-2/56L) match Mistral's released configs; Phi-3.5-MoE (4096/6400/16/top-2/32L) matches Microsoft's config; DeepSeekMoE (2048/1408/64/top-6/28L) reconstructs 16.4B, matching DeepSeekMoE-16B; Hunyuan-Large (6400/18304/16/top-1/64L) reconstructs 389.5B against Tencent's stated 389B. The GQA group parameter m also matches each model's Q/KV head ratio in every row (Mixtral-8x7B 32/8=4, 8x22B 48/8=6, Hunyuan 80/8=10). This makes Table 2 a reliable secondary cross-check source — notably the only citable source for Hunyuan-Large's expert width. Correctly notes the MFU values here are figure-only and reports them unknown.
[confirmed] MegaScale — 530B GPT-style transformer (DENSE): Reconciles at 528.5B vs stated 530B (-0.3%) from Table 1's dims (hidden 20480, 105 layers). The important judgement is a refusal and it is correct: the per-scale weak-scaling MFU values exist only in Figure 9, and the run explicitly declines to carry the third-party 54.30/54.10/54.30 numbers that circulate for it. That is exactly the right call — those cannot be verified against the paper, which offers only 'higher than Megatron-LM by up to 6.1%' and 'decreases by 1.6%'. Keep the row as a partial, text-only datapoint.
[confirmed] MoE Parallel Folding / Megatron-Core — Qwen2-57B-A14B: All Table 4 values transcribed correctly, and this is the widest MoE MFU-vs-scale curve available (five points, 64 -> 1024 GPUs, four parallelism strategies). Note the same batch-size resolution I established for the Mixtral row applies here: the 39.0% in the abstract/Table 3 is at global batch 256, while Table 4's 39.9% at 64 GPUs is at global batch 1024 — label them. Architecture fields are correctly reported as not stated in this paper (the 57B/14B/64-expert/top-8 figures come from Qwen's own report, which independently reconciles to 57.4B). The long-context result (38.7% -> 35.9% out to 128K) is a useful second axis.
[confirmed] MoE Parallel Folding / Megatron-Core — Mixtral-8x22B-G8T8: Values transcribed correctly, including the paper's own '18,4%' comma typo at 256 GPUs, which is preserved as printed rather than silently normalised — the right call. This is the most valuable row in the paper for the fine-grained-MoE argument: it is the same capacity as Mixtral 8x22B reparameterised to 64 experts with one-eighth expert width, and MFU collapses from 52.2% to 30.0% at 128 GPUs, with the baseline MCore falling to 19.8% and FSDP to 2.2%. Correctly reports total/active parameters and absolute widths as not stated — the paper gives only the 'one-eighth of the original' ratio.
[confirmed] Megatron-Core MoE technical report — DeepSeek-V3-685B: Table 11 is internally consistent to three digits, which is strong evidence the transcription is clean: TFLOPS divided by tokens/s/GPU gives 260.7, 260.7, 259.9 and 260.6 GFLOPs/token across the GB300, GB200-MXFP8, GB200-BF16 and H100 rows respectively — a constant, as it must be for one model. That value also squares with the 250 GFLOPS/token DeepSeek's own hardware paper reports for V3 once attention and MTP are included. Correctly states that no MFU accompanies these rows and that the 685B = 671B main + 14B MTP decomposition is the paper's own. arXiv:2603.07685 verified to exist with the exact stated title. The authors' own point-in-time caveat is properly carried.
[confirmed] Megatron-Core MoE technical report — Qwen3-235B: Same internal-consistency check passes: 148.0, 147.9, 147.1 and 150.1 GFLOPs/token across the four 4,096-sequence rows, consistent with 22B active parameters plus attention. The long-context row (128 GPUs, 131,072 sequence, 1,150 TF, 1,556 tokens/s/GPU) correctly implies a much larger per-token cost, as expected. Good discipline flagging that the 22B active figure is only implied by the single use of the full name 'Qwen3-235B-A22B' and is never stated numerically, and that the VPP06/VPP04 strings are transcribed exactly as printed — the sibling scaling-laws extraction independently reads them as VPP 6 and 4, which agrees.
[confirmed] Pangu Ultra MoE 718B [systems-papers]: Duplicate of the chinese-large and scaling-laws rows for the same run — dedupe to one entry. Content verified as above (719.1B calc vs 718B stated, 39.2B vs 39B active; MFU/TPS pair internally consistent at both 6K and 4K NPU configurations). This copy adds the useful and correct warning that the 30.0%/6K and 18.9%/4K comparison differs in BOTH software stack and cluster size, so it is not a clean MFU-vs-scale curve and must not be plotted as one. That caveat should survive the merge.
[confirmed] DeepSpeed-MoE — 1.3B+MoE-128 (52B total): Reconciles at 52.6B vs stated 52B (+1.1%) with an ungated GeLU FFN over 12 MoE layers of 24 — the era-appropriate 2-matrix FFN, correctly implied by the run's own description. Metric is correctly identified as training samples per second (372 vs the dense baseline's 70), not MFU or tokens/s, and the refusal to convert to tokens/s despite the stated 2K sequence length is the right restraint since the paper never does that conversion. moe_intermediate is genuinely absent from the paper and correctly left unknown.
[confirmed] DeepSpeed-MoE — 6.7B dense baseline: Straightforward and correct: 32 layers, hidden 4096, 300B tokens, 2K sequence, 128 A100s, 70 training samples/sec. Valuable specifically as a quality-matched dense contrast trained on the same hardware, same token budget and same framework as the 1.3B+MoE-128 — that controlled pairing is rare and is the row's whole point. Parallelism degrees genuinely unstated.
[confirmed] Megatron-LM PTD-P (Narayanan et al. 2021) — 1.7B to 1T weak-scaling suite: All ten rows verified arithmetically: 12 x hidden^2 x layers reproduces every stated parameter count within 1.2%, and every printed 'percentage of theoretical peak' equals the row's TFLOPs/GPU over the stated 312 TF A100 peak (137->43.9%, 142->45.5%, 155->49.7%, 163->52.2%). The metric caveat is the single most important thing in this row and is correct: the paper's FLOP formula explicitly assumes activation recomputation and counts the extra forward pass, so these percentages are HFU-flavoured and must NOT be plotted against the MFU numbers from MegaScale, MegaScale-MoE or MoE Parallel Folding without adjustment. Pulling Table 1 from the PDF because the arXiv HTML renders it as an image was the right workaround.
[confirmed] Tutel — single MoE layer scaling microbenchmark: Accurately reported as a single-layer microbenchmark: no total or active parameters exist, and the metric is step time and speedup ratios only (4.96x / 3.11x / 5.75x at 16 / 128 / 2,048 GPUs) with no MFU, TFLOPS or tokens/s. The scale-dependent decomposition is the useful content — kernel optimisations give 3.52x at 16 GPUs but only 1.04x at 2,048, while 2DH All-to-All gives 4.25x at 2,048 — which is a genuine communication-vs-scale datapoint. Testbed and interconnect are unusually well specified. Contributes nothing to the model/tokens/parallelism columns; keep as a systems citation.
[confirmed] Tutel — SwinV2-MoE: Correctly reported, but note it is a vision model measured in images per second, so it contributes to none of the four requested throughput units and has no token count in the LLM sense. Total and active parameters are genuinely not stated numerically in the extracted text. The 32-GPU figure is correctly scoped to the 8- and 16-expert configurations only. Marginal for a language-model training table — include only if the paper wants a cross-modality point.
[confirmed] MegaBlocks — dMoE (MoE-XS / Small / Medium): Correctly reports that no absolute throughput metric exists — no MFU, HFU, TFLOPS or tokens/s, only end-to-end training time and speedup ratios (1.38x/2.0x/4.35x vs Tutel padding; 1.8x-2.4x vs dense Megatron-LM at equal validation loss). Architecture is fully specified for the dense bases and the 4x-hidden expert rule, tokens (10B), sequence length (1024), batch (512 sequences) and the exact parallelism (EP=8, DP=8, TP=1, PP=1) are all stated. Total parameters for the MoE variants are genuinely not given and correctly left unknown.
[confirmed] HierMoE — scaled-down DeepSeek-V3 and Qwen3-30B-A3B: The critical caveat is correct and prominently placed, which matters more here than anywhere else in the systems set: these are NOT the real models. The 'DeepSeek-V3' run is a 6-layer, half-width surrogate and the hardware is 32 RTX A6000 prosumer cards, not datacentre parts. Any table row must say so or it will be read as a DeepSeek-V3 training measurement. Throughput is speedup ratios only (1.18x-1.27x end-to-end, 1.55x-3.32x communication) with no MFU/TFLOPS/tokens-per-second. arXiv:2508.09591 verified to exist with the stated title. The scale-dependence note (speedup falls as G rises; at G=8 there is no inter-node communication) is the useful content.
[confirmed] Lina — Transformer-XL / BERT2GPT2 / GPT-2 converted to MoE: Accurately reported as a partial: no parameter counts, no widths, no token count, no sequence length or batch, and throughput is ratios and times only (at least 2.21x all-to-all, 1.57x average step time, 1.45x/1.63x inference) with no MFU/TFLOPS/tokens-per-second. The two supporting measurements are the genuinely citable content — all-to-all consuming 34.1% of step time and 3.7% average GPU SM efficiency during all-to-all. Testbed (16 A100 40GB, 100 Gbps InfiniBand) is properly specified. Keep as a communication-overhead citation.
[confirmed] SmartMoE — GPT-MoE and Swin-MoE: Correctly reported, and the flagged ambiguity is genuine and honestly surfaced rather than resolved by guessing: Table 2 lists the inky cluster's maximum as 32 GPUs while the results figure carries 64-GPU labels, and the extraction declines to assign columns to clusters. That is the right call — leave it ambiguous. Throughput is training latency and speedup ratios versus FasterMoE only (1.53x average on inky, 1.17x on pinky, 1.88x maximum), with no MFU/TFLOPS/tokens-per-second. Parameter counts are given per weak-scaling step (4.5B/7.3B/9.9B/14.0B) but widths, expert counts and top-k are not.
[confirmed] FSMoE — GPT2-XL-based and Mixtral-based MoE models: Accurately reported. All architecture fields are swept ranges rather than concrete model configurations (M in {256..2048}, H in {1024, 2048, 4096}), so no parameter recompute is possible and none is claimed. Hardware is consumer/prosumer (RTX A6000 and RTX 2080Ti), which should be stated in any table row since it makes the absolute numbers non-comparable to datacentre results. Throughput is iteration-time breakdowns and speedup ratios only. Genuinely useful as the open, citable stand-in for the inaccessible ScheMoE from the same group, and as a source for the >50% communication-overhead claim.
[confirmed] Ling-Plus / Ling-Lite (Ant Group) [systems-papers]: Accurately reported; merge with the richer scaling-laws copy, which adds the batch warmup (2,560 -> 8,960), the 4K->16K sequence extension and the 150B long-context tokens. The defining feature is correctly captured: accelerators are deliberately anonymised as devices A-E by peak FLOPS, memory and cost, so accelerator type is unobtainable by design rather than by omission, and there is no MFU, HFU, TFLOPS or tokens/s anywhere — the efficiency metric is RMB per trillion tokens (6.35M on device D vs 5.08M on lower-spec, ~20% saving). The tp/ep/pp fragments are correctly scoped to a checkpointing table rather than the pretraining run.
[confirmed] Chinchilla 70B (Hoffmann et al. 2022): Reconciles at 64.9B vs stated 70B (-7.2%), within tolerance for a 2-matrix GeLU FFN at 4x d_model over 80 layers (the residual is relative-positional-encoding and vocabulary accounting the arch table does not itemise). Correctly reports that the paper gives no accelerator count and no throughput metric of any kind — TPUv3/TPUv4 is named but the mix is not broken out. Include as the dense token-budget reference it is, not as a throughput datapoint; its value to the paper is the 1.4T-token/70B allocation and the 1.5M->3M batch doubling.
[confirmed] PaLM 540B (Chowdhery et al. 2022): Reconciles at 567.6B vs stated 540.35B (+5.0%) under a SwiGLU-4d reading. I verified the paper's own MFU arithmetic from the quoted expression and it is exact: (238.3e3 x 6 x 540e9) / (275e12 x 6144) = 45.70%, matching the printed 45.7% excluding self-attention and 46.2% with it. This is one of only three rows in the corpus carrying MFU, HFU and tokens/s together (46.2% / 57.8% / 238.3K tokens/sec), which makes it the reference point for the MFU-vs-HFU distinction. Cluster, parallelism and batch schedule are all fully specified.
[confirmed] Megatron sequence-parallelism benchmarks: 22B / 175B / 530B / 1T (Korthikanti et al. 2022): All four parameter counts verified: 12 x hidden^2 x layers gives 21.7B / 173.9B / 528.5B / 1006.6B against stated 22B / 175B / 530B / 1T, all within 1.2%. The highest-value row in the scaling-laws stream for the paper's throughput argument, because it reports MFU and HFU side by side across a 4x model-size range on identical hardware (41.5/43.7, 51.4/52.8, 56.0/57.0, 56.3/57.0) — that pairing is what lets a reader convert between the recomputation-inclusive numbers in Megatron-LM PTD-P and the MFU numbers elsewhere. The DP=1 caveat and the 2240-GPU data-parallel extension (56.0% -> 54.2%) are correctly captured.
[confirmed] MegaScale 175B strong-scaling study [scaling-laws]: Duplicate of the systems-papers MegaScale 175B row — merge. Notably, this copy omits the aggregate-PFLOP/s column and therefore does not carry the defective 2166.3 cell; its MFU and tokens/s values are the self-consistent ones I verified. Prefer this copy's numbers. All eight MFU/tokens-per-second pairs at both batch sizes check out against iteration time and batch. The quoted causal explanation (fixed batch, falling computation-to-communication ratio) is the mechanism the paper needs.
[confirmed] DeepSeek-V3 (DeepSeek-AI 2024) [scaling-laws]: Duplicate of the deepseek-qwen DeepSeek-V3 row — merge. Content verified identically (670.9B calc vs 671B; 36.5B vs 37B active; derived 1,543 tokens/s/GPU exact). This copy independently reaches the same correct conclusion on the key judgement: the tech report contains no MFU or HFU, and converting the GPU-hour cost rate into an MFU is refused for want of a stated peak-FLOPS denominator and a defensible FLOPs/token term. Two independent extractions agreeing on that refusal is a good signal.
[confirmed] Skywork-MoE 146B (Wei et al. 2024) [scaling-laws]: Duplicate of the chinese-large Skywork-MoE row — merge; the two agree on every field. Verified: 146.4B calc vs 146B stated, 22.4B vs 22B active, and the 12 x 4 x 32 = 1536 parallelism/GPU-count cross-check. Retains the same high value as one of the few rows with an explicitly-labelled MFU (38%) alongside tokens/GPU/s (690) and complete parallelism and interconnect detail.
[confirmed] MegaScale-MoE Internal-352B (ByteDance 2025) [scaling-laws]: Duplicate of the systems-papers MegaScale-MoE Internal-352B row — merge; the two agree, and this copy adds the paper's MoE-specific hardware insight verbatim (MFU falls as GPU compute capability rises because MoE routing, local scatter and gather are memory-bound and memory bandwidth scales more slowly than compute), which is directly useful to a systems paper's argument. Arithmetic verified as above (341.3B vs 352B; both MFU endpoints and all five throughput/training-time pairs consistent).
[confirmed] Mixtral 8x22B under MoE Parallel Folding [scaling-laws]: Duplicate of the systems-papers MoE Parallel Folding Mixtral 8x22B row — merge, and prefer THIS copy on the disputed point: it correctly states the scaling experiments use global batch 1024, which is what resolves the 49.3%-vs-52.2% discrepancy the systems-papers copy left open. I confirmed that sentence verbatim in the arXiv source. All Table 4 and Table 5 values verified, and the hardware specification (989.5 TFLOP/s peak, 450 GB/s NVLink, 400 Gbps InfiniBand, BF16 throughout) is complete.
[confirmed] Qwen2-57B-A14B under MoE Parallel Folding [scaling-laws]: Duplicate of the systems-papers MoE Parallel Folding Qwen2-57B-A14B row — merge; values agree exactly across both extractions, which is good corroboration for a five-point, four-strategy MFU curve. This copy correctly states the global batch as 1024 for the scaling sweep. Note the paper's 57B/14B/64-expert/top-8 description matches Qwen's own report, which independently reconciles to 57.4B.
[confirmed] DeepSeek-V3-685B under Megatron-Core MoE [scaling-laws]: Duplicate of the systems-papers Megatron-Core row — merge; both agree on all four Table 11 rows, which I verified are mutually consistent at ~260.7 GFLOPs/token. This copy adds the useful memory-driven configuration insight verbatim (GB200's 192 GB per GPU versus H100's 80 GB permits TP1/PP4 instead of TP2/PP8), which explains the H100 row's much lower per-GPU throughput. Correctly notes no MFU accompanies these rows and that force-balanced routing removes the routing imbalance a real run would face — an important comparability caveat.
[confirmed] Qwen3-235B under Megatron-Core MoE [scaling-laws]: Duplicate of the systems-papers Megatron-Core Qwen3-235B row — merge; both agree on all five Table 11 rows, verified mutually consistent at ~148 GFLOPs/token for the 4,096-sequence rows. Reads the VPP strings as 6, agreeing with the sibling extraction's reading of the ambiguous 'VPP06'. The >100% relative long-context MFU note is correctly quoted with the paper's own explanation (SDPA kernels dominate and are highly efficient at long sequences), which prevents it being misread as an error.
[confirmed] Hunyuan-Large 389B (Tencent 2024) [scaling-laws]: Duplicate of the chinese-large Hunyuan-Large row — merge, and apply the same fix: moe_intermediate can be filled with 18304 from MegaScale-MoE Table 2, which I confirmed is the only value reconciling Tencent's stated 389B (389.5B calc, +0.1%). Otherwise verified: Table 1 transcribed correctly, 7T tokens, 1 shared + 16 specialized with top-1 activation, and the correct negative finding that the report has no infrastructure section at all — no hardware, no MFU, no parallelism.
[confirmed] Clark et al. routed-language-model sweep (2022): Accurately reported. Architecture sweep is fully specified across all seven base sizes with exact non-embedding parameter counts, and the training protocol is unusually precise (130B tokens for every model regardless of N, 250,000 steps x batch 256 x sequence 2048, 168 models total). Correctly reports no throughput of any kind and no accelerator count — 'TPUs' is named without a type. Contributes to the scaling-law axis, not the throughput axis; the parallelism note (4 or 8 local experts per device) is the only hardware-adjacent detail.
[confirmed] Ludziejewski et al. fine-grained-MoE granularity sweep (2024): Accurately reported. The only throughput-adjacent content is correctly characterised as a qualitative wall-clock observation on a single A100 (G=8 best, G=16 dominated by routing cost) rather than a throughput measurement — no MFU or TFLOPS exists. Sweep scope (129M-3.7B parameters, 16B-130B tokens, over 100 experiments, E fixed at 64, granularity 1 to 16) is well specified. The granularity finding is directly relevant to the fine-grained-MoE efficiency collapse that MoE Parallel Folding's Mixtral-8x22B-G8T8 row measures on real hardware — worth pairing them in the paper.
[confirmed] Abnar et al. optimal-sparsity sweep (2025): Accurately reported, including the correct negative finding that the paper has no hardware section at all — no accelerator, no count, no throughput. Method detail is sufficient (MegaBlocks dropless MoE, sparsity swept over [0, 0.98] with expert count and top-k adjusted to hit targets, batch fixed at 1024, RedPajamaV1, GPT-NeoX tokenizer). Purely a scaling-law citation for the parameters-versus-FLOPs tradeoff; no arithmetic check is applicable since no specific model is trained.
[confirmed] Ludziejewski et al. Joint MoE Scaling Laws sweep (2025): Accurately reported with the fitted coefficients transcribed in full and the reported fit quality (RMSE 0.0039 held-out, 0.0062 training). Correctly reports no accelerator and no throughput for the runs themselves — H100/8xH100/RTX4090 appear only as memory constraints in the analysis. The quoted mechanism (limited GPU memory forcing small batch sizes and hence low hardware utilisation) is the conceptual bridge between the scaling-law stream and the MFU measurements elsewhere in the corpus, and is worth citing for exactly that.
[confirmed] Kaplan et al. dense scaling-law sweep (2020): Accurately reported as the dense baseline. No hardware, no throughput, no parallelism — correctly stated. Scope (roughly 768 to 1.5B non-embedding parameters, WebText2, 1024-token context, vocab 50257) is right. Its role in this corpus is correctly identified: the critical-batch-size law is the ancestor of the per-device-batch arguments that drive the MFU-versus-scale behaviour measured in MegaScale, MegaScale-MoE and MoE Parallel Folding.
[confirmed] Zhang et al. critical-batch-size sweep (2024/25): Accurately reported. Model sizes (85M to 1.2B), dataset (C4) and the Chinchilla-optimal token schedule with 0.28x/0.5x/2x/4x step variants are specified. Correctly reports no throughput metric — the paper is about the batch-size/steps tradeoff, not hardware utilisation — and no accelerator in the extracted sections. The headline finding (critical batch size scales primarily with data size rather than model size) is the quantitative backing for why fixed-global-batch strong-scaling curves degrade, which is exactly the effect the MegaScale and MegaScale-MoE tables exhibit.
[confirmed] Llama-13B / 30B / 65B parallelisation-layout sweep (Hagemann et al. 2024): Accurately reported and directly on-point for the paper's parallelism axis. Reports MFU explicitly (70.5% for Llama-13B, 59.6% at 65B) with the published comparison points (53.3% MPT-70B, 49.4% Llama-65B, 34.7% Megatron-LM 76B) and a full Cartesian sweep of TP/PP/micro-batch/activation-checkpointing on 64-256 A100s with complete interconnect detail. The central finding quoted (micro-batch size 1 achieves the highest MFU; smaller micro-batches correlate with better MFU) is the cleanest published statement of the per-device-batch effect. Note the 30B model's 52 attention heads are not divisible by 8, which the run correctly flags as constraining the TP sweep.
[confirmed] DeepSeek LLM 7B/67B scaling-law study (2024): Accurately reported, including the correct negative finding that the infrastructure section describes the HAI-LLM framework's capabilities (DP, TP, SP, 1F1B PP, flash attention, ZeRO-1, bf16 with fp32 gradient accumulation) while naming no GPU type, no count and no MFU/TFLOPS/tokens-per-second. Overlaps the deepseek-qwen 'DeepSeek LLM 67B (DENSE)' row — merge, taking the architecture and the 300.6K GPU-hour figure from that row and the scaling-law content (compute budgets 1e17 to 3e20 FLOPs, ~10 allocations per budget, 100M-token validation set) from this one. Its citable value is the batch-size and learning-rate scaling laws.